# Парсинг неструктурированных логов

`scripts/parse_logs.py` — stdlib-only, Python 3.8+. Задача скрипта: превратить произвольную кучу строк в короткую сводку, по которой можно рассуждать, и в набор **сигнатур** для поиска по базе знаний.

## Что он делает

1. **Читает** файлы, glob-маски, директории, `.gz`/`.bz2`/`.xz`/`.zip`, а также stdin (`-`).
2. **Определяет формат каждой строки** независимо — в одном файле спокойно уживаются JSON-строки от одного сервиса и nginx-access от другого:
   - JSON lines (`{"level":"error","ts":...}`) — поля `time/timestamp/@timestamp/ts/date`, `level/severity/lvl`, `message/msg/log/short_message`;
   - logfmt (`level=error msg="..." dur=1.2s`);
   - nginx/apache combined и common;
   - syslog (`Jul 28 12:33:44 host app[123]:`);
   - «человеческий» текст с таймстемпом и уровнем в начале строки;
   - всё остальное — как plain-запись.
3. **Склеивает многострочные записи.** Строка считается началом новой записи, если в её начале есть таймстемп, или это JSON-объект, или она подходит под access-log. Всё прочее — продолжение предыдущей. Так стектрейсы Java/Python/Go, SQL-дампы и XML не рассыпаются на сотни «ошибок».
4. **Нормализует уровень**: `WARN/WARNING/W`, `ERR/ERROR/E`, `FATAL/CRIT/CRITICAL/PANIC/EMERG` и т.д. приводятся к общей шкале. У access-логов уровень выводится из HTTP-статуса (5xx → ERROR, 4xx → WARN).
5. **Шаблонизирует сообщения**: маскирует изменчивые части — числа, UUID, IP, hex-строки, кавычки, пути, URL, email, длинные идентификаторы. `user 8213 not found` и `user 91 not found` схлопываются в один шаблон `user <n> not found` с счётчиком 2.
6. **Считает статистику**: частота шаблонов, first/last seen, гистограмма по минутам, распределение уровней, топ exception-классов, HTTP-статусы.
7. **Извлекает сигнатуры** — компактные строки вида `java.sql.SQLTimeoutException`, `http_status=504`, `tmpl:connection pool exhausted for <str>`. Это то, что переживает смену версий и по чему база знаний ищет похожие случаи.

## Использование

```bash
# базовая сводка
python3 scripts/parse_logs.py app.log

# только ошибки, топ-15 шаблонов
python3 scripts/parse_logs.py 'logs/**/*.log' --level ERROR --top 15

# окно времени
python3 scripts/parse_logs.py app.log --since "2026-07-28 12:00" --until "2026-07-28 13:00"

# из stdin
kubectl logs -n stage payment-api --since=1h | python3 scripts/parse_logs.py -

# машинный вывод для kb_search / kb_add
python3 scripts/parse_logs.py app.log --format json > /tmp/parsed.json

# сырые записи конкретной группы (номер из сводки)
python3 scripts/parse_logs.py app.log --context 3

# всё по одному request/trace id
python3 scripts/parse_logs.py app.log --trace 7f3a9c21
```

Флаги: `--level`, `--since`, `--until`, `--grep`, `--top`, `--context N`, `--trace ID`, `--histogram`, `--show-info`, `--format md|json`, `--max-chars`, `--max-lines`, `--encoding`.

Два из них нужны не всегда, но вытаскивают из тупика:

- `--show-info` добавляет в сводку раздел «Прочие сообщения» — топ-5 шаблонов, которые не ошибки и не предупреждения. Повод: ошибок не нашлось вовсе или падение логировалось как INFO (обрыв лога, OOM, kill снаружи).
- `--max-chars N` задаёт предел объёма сводки в символах; по умолчанию — `MAX_SUMMARY_CHARS`, `0` снимает предел. Предел существует потому, что сводка едет в контекст агента и остаётся там до конца разбора; снимать его стоит только при выводе в файл, а не в контекст.

## Как читать сводку

- **Доминирующий шаблон важнее редкого.** Одинокий exception на фоне 4000 однотипных таймаутов — обычно следствие, а не причина.
- **First seen** — точка старта. Сверь с деплоем, релизом, словами пользователя. Если ошибки шли и до инцидента — они, вероятно, фоновый шум и не относятся к делу.
- **Всплеск на гистограмме** локализует момент. Ровный фон весь период — проблема не новая.
- **Первая ошибка в цепочке trace id** ближе к причине, чем последняя. Смотри `--trace`.
- **Пустой результат по ERROR** — не значит «всё хорошо». Перезапусти без `--level`: падение могло логироваться как INFO, или процесс убили снаружи (OOM, SIGKILL) и в логе просто обрыв.

## Время: шкала, зоны, допущения

Порядок событий — то, на чём держится весь разбор: вероятной причиной называется самое раннее новое событие. Ошибка в разборе времени не видна в выводе — он выглядит как обычно и указывает не на тот сервис. Поэтому со временем скилл обращается так:

- **Одна шкала — UTC.** Все времена в сводке, ленте и цепочке показаны в ней, и она названа в выводе. Времени без указания шкалы в отчёте быть не должно: читатель не сможет сверить его с Kibana или с текстом алерта.
- **Смещение, записанное в логе, учитывается.** `Z`, `+03:00`, `+0300`, `-05` в конце таймстемпа разбираются вместе со временем, и запись приводится к UTC. Поэтому `12:00:00Z` от gateway и `12:00:00+03:00` от payment-api встают в правильном порядке, а не выглядят одновременными.
- **Зона не домысливается.** Если в записи её нет, время остаётся как записано, а запись помечается признаком «зона неизвестна». Подставлять зону машины разбора нельзя: результат стал бы зависеть от того, с чьего ноутбука запущен разбор. Подставлять зону соседних записей — то же самое, только незаметнее.
- **Смешение с зоной и без неё — риск, а не поломка.** Когда в разбор попали и те и другие, сводка говорит об этом прямо и называет последствие: порядок между двумя группами может быть неверен, а с ним и вывод о том, что упало первым. Починить это нечем — зону наивных записей взять неоткуда, — поэтому оговорка идёт в ответ пользователю.
- **Эпоха читается как UTC.** Секунды эпохи по определению отсчитываются от UTC, поэтому epoch-записи сходятся с ISO-записями того же кластера независимо от зоны машины разбора.
- **Год в syslog выбирается «не в будущем».** В `Jul 28 12:33:44` года нет. Берётся такой, чтобы событие не оказалось позже момента разбора: месяц и день больше сегодняшних — значит, прошлый год. Подставленный год называется в сводке как допущение. Правило ошибается только на логах давностью больше года.

Допущения о времени видны в сводке разделом «Допущения о времени», а в JSON-выводе — полями `time_scale`, `tz_known`, `tz_unknown`, `mixed_timezones`, `time_assumptions`. Разбор, сделанный прежней версией скилла, этих полей не имеет и читается как «зона неизвестна» — то есть ровно так, как он и был получен.

Время скилл **не правит сам**. Расхождение часов между источниками он обнаруживает и называет, а поправку применяет только заданную явно — см. `references/correlation.md`.

## Ограничения, о которых стоит помнить

- Разбираются только числовые смещения, записанные в самом логе. Именованных зон (`MSK`, `Europe/Moscow`) скилл не знает: базы зон в поставке нет и внешних зависимостей он не тащит.
- Шаблонизация агрессивна: очень разные сообщения с одинаковым скелетом могут слиться. Перед выводом всегда проверяй сырые записи через `--context`.
- Бинарный мусор и очень длинные строки обрезаются (по умолчанию 8 КБ на строку).
