## 1. Непроверяемое на этой машине — принятый риск

Прогоны клиента (`--exclude-tools`, `--allowed-mcp-server-names`, `--sandbox`, подхват
скилла в headless) выполнить негде: gigacode есть только во внутреннем контуре. Риск
принят сознательно. Вместо прогона — сделать так, чтобы расхождение чинилось настройкой и
было видно, а не приводило к тихо неверному разбору.

- [x] 1.1 Профиль запуска задаёт все ограничения **явно**, включая режим одобрения: ничего
      не наследуется из настроек машины, где значение может отличаться
- [x] 1.2 Каждое ограничение в `references/platforms.md` помечено состоянием проверки;
      непроверенное названо предосторожностью, а не защитой — с датой и версией сборки
- [x] 1.3 Команда запуска в документации собрана так, чтобы отсутствующий у сборки флаг
      приводил к отказу запуска, а не к молчаливому запуску без ограничения
- [x] 1.4 `--append-system-prompt` с явным указанием разобрать инцидент входит в команду
      запуска: подхват скилла по `description` не предполагается
- [x] 1.5 В `references/aiops.md` — раздел «если поведение клиента отличается»: таблица
      «признак в выводе клиента → что поправить в профиле запуска». Раздел лежит в скилле,
      а не в документации репозитория: им пользуются и дежурный, и агент по ходу разбора
- [x] 1.6 Тот же раздел покрывает случай «разбор не начался»: признаки того, что скилл не
      подхватился или скрипты не запускались, и что поправить в команде

## 2. Нормализация алерта

- [x] 2.1 `scripts/alert_to_incident.py`: чтение payload со stdin, распознавание форматов
      Alertmanager, Grafana и Sentry, вывод карточки и аргументов разбора
- [x] 2.2 Нераспознанный формат и отсутствующие поля: ненулевой код и явная отметка вместо
      подстановки значений по умолчанию
- [x] 2.3 Окно разбора строится вокруг времени инцидента из алерта; при заданном
      `INCIDENT_NOW` результат воспроизводим
- [x] 2.4 Тесты нормализатора: три формата, отсутствующие поля, мусор на входе

## 3. Машинный отчёт

- [x] 3.1 `scripts/triage.py`: сборка `report.json` в директории вывода — вердикт, уровень
      уверенности, сигнатура, доказательства с источниками, запись базы знаний, черновик
      следующего шага
- [x] 3.2 Вердикт «данных недостаточно» как значение поля отчёта с перечнем недостающего;
      коды возврата под это не занимаются
- [x] 3.3 Пройденные и пропущенные контуры с причинами попадают в отчёт
- [x] 3.4 Очистка доказательств теми же правилами, что запись базы знаний; сырые дампы в
      отчёт не попадают
- [x] 3.5 `templates/aiops-report.md` — описание полей отчёта
- [x] 3.6 Тесты состава отчёта: полный разбор, разбор без логов, ограниченный режим

## 3a. Диагностика прогона

- [x] 3a.1 В команде запуска и в `references/aiops.md` зафиксировать: `INCIDENT_TRACE_FILE`
      указывает рядом с отчётом, поток сессии клиента сохраняется в ту же директорию,
      `--session-id` равен идентификатору алерта
- [x] 3a.2 Отчёт называет пути телеметрии и сессии, чтобы по одному файлу находились
      остальные
- [x] 3a.3 Проверить на прогоне, что по телеметрии различимы пропущенный и упавший контур

## 4. Поведение скилла в автономном режиме

- [x] 4.1 `references/aiops.md`: признак режима, таблица «вопрос → правило вывода»,
      потолок прохода, требования к среде запуска, готовая команда запуска с реальными
      путями и путём отчёта
- [x] 4.2 `SKILL.md`: ветка автономного режима в шагах 0, 2, 3, 8 и 9 — без дублирования
      текста, ссылкой на `references/aiops.md`
- [x] 4.3 Разбор `INCIDENT_MODE` в `kb_common`: только известные значения, неизвестное —
      ошибка запуска
- [x] 4.4 `scripts/kb_add.py`: в автономном режиме путь только из `--kb` или
      `INCIDENT_KB_DIR`, отсутствие пути — не ошибка, а отметка в отчёте
- [x] 4.5 Повтор известной сигнатуры обновляет запись базы знаний вместо создания новой
- [x] 4.6 Тесты: запись без заданного пути не создаётся, повторная сигнатура обновляет
      существующую запись

## 5. Реестр платформ и среда запуска

- [x] 5.1 `references/platforms.md`: раздел о режимах запуска — что проверено прогоном,
      с датой и версией сборки, и что описано по документации
- [x] 5.2 Там же: правило личной установки под автономный профиль и состояние его проверки
- [x] 5.3 Перечень рычагов ограничения по убыванию надёжности; непроверенное не называется
      защитой

## 6. Документация

- [x] 6.1 `README.md`: раздел о режимах запуска, команда с реальными путями, путь и состав
      файла отчёта, граница ответственности среды
- [x] 6.2 `AI_CONTEXT.md`: раскладка (новые скрипты и `references/aiops.md`), команды,
      ограничения автономного режима
- [x] 6.3 `docs/walkthrough.md`: прогон от алерта до `report.json` на материалах
      репозитория
- [x] 6.4 `docs/presentation.html`: слайды автономного сценария — вход, выход, условие
      применения; числа с указанием инструмента, которым получены

## 7. Приёмка

- [x] 7.1 `python3 tests/run.py` — весь набор проверок зелёный
- [ ] 7.2 `python3 tools/bench.py` — бюджеты не нарушены

      Прогнано: нарушен один бюджет — `parse_logs.py --level ERROR` даёт 145 тыс. строк/с
      при пороге 150 тыс. Нарушение **не связано с этим изменением**: тот же замер на
      дереве до правок (`git stash`) даёт тот же результат. Порог машинозависимый, и
      разбираться с ним надо отдельно, а не подгонять под текущую машину.
- [x] 7.3 `npx @fission-ai/openspec@latest validate --specs --strict`
- [x] 7.4 Сквозной прогон автономного режима на материалах репозитория: алерт → команда
      запуска → `report.json` с вердиктом
- [x] 7.5 Прогон отрицательного сценария: логов нет — отчёт с вердиктом «данных
      недостаточно», а не догадка
