## 1. Проверка синхронности и выравнивание таблицы контуров

- [ ] 1.1 `tools/check_docs.py`: проверка синхронности таблицы трёх контуров — парсинг
      канона в `incident-detective/SKILL.md`, сверка перечня контуров и состава скриптов
      с `README.md`, `AI_CONTEXT.md` и таблицей `docs/presentation.html`; расхождение —
      ненулевой код с указанием документа. На текущих копиях проверка красная
- [ ] 1.2 Выровнять копии по канону: README и AI_CONTEXT — убрать `kb_index.py` из
      строки контура базы знаний, презентация — дополнить состав скриптов; проверка 1.1
      зелёная
- [ ] 1.3 Рядом с каждой витринной копией — пометка о каноне (в README уже есть,
      добавить в AI_CONTEXT; для презентации — комментарий в html)

## 2. Дедупликация документации

- [ ] 2.1 SKILL.md: перечень маскируемых данных в шаге 9, разделе постмортема и
      «Границах» заменить ссылками на `kb-format.md#маскирование`; пересказ в
      `references/next-step.md` §5 — тоже ссылкой. Перечень видов и форма меток остаются
      только в каноне
- [ ] 2.2 SKILL.md: правила отличительных признаков — полный текст один раз (шаг 4),
      в шаге 1 строка со ссылкой; «лишняя задача — лишний проход модели» — один раз
      (шаг 0); пересказ onboarding.md в шаге 2 сжать до правила плюс ссылки; список
      ключевых слов шага 3 → ссылка на `log-sources.md` §5
- [ ] 2.3 SKILL.md: шаг 8 «Где держать базу» сжать до триггера (код 3), формы вопроса
      и ссылки на `kb-format.md#где-лежит-база`; повторы правил ограниченного режима
      свести к одному упоминанию на шаг со ссылкой на `limited-mode.md` §4–5. Сверить
      по перечню находок, что каждое снятое правило живёт в каноне
- [ ] 2.4 Удалить `incident-detective/templates/incident.md`; убрать строку из дерева
      файлов README — `check_docs` зелёный
- [ ] 2.5 `docs/walkthrough.md`: пометка у 8-пунктового плана — канон в шаге 0 SKILL.md
- [ ] 2.6 «GigaCode — калька Qwen Code»: полный абзац — только в `AI_CONTEXT.md`;
      README и `openspec/config.yaml` — сжатая фраза со ссылкой на канон

## 3. Дедупликация кода

- [ ] 3.1 `parse_logs.py`: `group_size`+`fit_groups` → `kb_common.fit_by_render`
      (рендер — `render_groups`); эталоны тестов не меняются. Удалить мёртвое поле
      `Group.traces`
- [ ] 3.2 `kb_search.py`: inline-цикл усечения выдачи → `fit_by_render`
- [ ] 3.3 `code_hints.py`: `limit_frames` внутри через `fit_by_render`, сигнатура и
      значения для JSON сохраняются; двойной вызов за запуск устранить — `build()`
      считает один раз
- [ ] 3.4 `code_hints.py`: кэш содержимого файлов для `grep_repo` на процесс (walk и
      чтение — один раз за запуск); разбор `%h|%ad|%an|%s` — одна функция для
      `commits_in_window` и `file_history`
- [ ] 3.5 `timeline.py`: `render_md` рендерит события через `render_event` c
      gap-суффиксом; замер бюджета учитывает суффиксы. Регресс-тест: вывод ленты на
      фикстурах не меняется
- [ ] 3.6 `parse_source_arg` → общий модуль; `trace.py` и оба inline-разбора
      `label=path` в `timeline.main` используют его; тест на неизменность меток
      источников при явном `label=`

## 4. Тесты

- [ ] 4.1 `tests/test_presentation.py`: убрать `test_main_returns_zero` (полный
      `check_docs.main` уже гоняет `run.py`), оставить `test_file_exists` и точечный
      тест `check_presentation(failures)`
- [ ] 4.2 Прогон `python3 tests/run.py` — весь набор зелёный, эталоны не менялись

## 5. Спеки и приёмка

- [ ] 5.1 Дельты log-ingestion, source-map, runtime-preflight, project-documentation:
      `npx @fission-ai/openspec@latest validate --specs --strict` проходит
- [ ] 5.2 `python3 tools/bench.py` до и после: бюджеты не нарушены, сводки совпадают
      по результатам (grep_repo быстрее — допустимо)
- [ ] 5.3 Финальная сверка по перечню находок аудита: каждая копия либо устранена,
      либо помечена как витринная с каноном и проверкой синхронности; поведение агента
      и скриптов не изменилось
