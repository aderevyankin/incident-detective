## 1. Общая функция очистки

- [x] 1.1 Перенести правила и функцию очистки из `kb_add.py` в `kb_common.py`
- [x] 1.2 Сохранить действующее поведение по секретам, подписанным именем поля
- [x] 1.3 Возвращать вместе с очищенным текстом сводку срабатываний по видам
- [x] 1.4 Обеспечить стабильность: одинаковое значение в пределах одной записи маскируется
      одинаково, сопоставление никуда не сохраняется

## 2. Правила распознавания

- [x] 2.1 Адреса электронной почты
- [x] 2.2 Номера телефонов в распространённых форматах записи
- [x] 2.3 Номера платёжных карт с проверкой контрольной суммой, с сохранением последних четырёх цифр
- [x] 2.4 Номера счетов и идентификаторы физических лиц
- [x] 2.5 Токены, узнаваемые по форме: значения с признаками ключа, подписанные строки
- [x] 2.6 Учётные данные внутри URL
- [x] 2.7 Починить разбор пар «ключ: значение» со служебным словом между ними:
      в `Authorization: Bearer <токен>` маскируется токен, а не слово `Bearer`

## 3. Перечень немаскируемого

- [x] 3.1 Зафиксировать список: идентификаторы запросов и trace id, номера заказов и транзакций,
      имена сервисов и хостов, версии, пути в коде, IP-адреса
- [x] 3.2 Проверить каждое правило распознавания против этого списка на ложные срабатывания
- [x] 3.3 Проверить, что после очистки записи остаются пригодными для поиска по сигнатурам

## 4. Применение на путях наружу

- [x] 4.1 `kb_add.py` — очистка всех сохраняемых текстовых разделов и полей, включая
      `--title`, `--signature` и значения из `--from-parsed` (сейчас не чистятся)
- [x] 4.2 Показ сводки срабатываний пользователю при записи
- [x] 4.3 Отражение маскирования в `--dry-run`: показывается уже очищенный текст
- [x] 4.4 Правило очистки текста тикета перед показом — в `SKILL.md` и `references/next-step.md`
- [x] 4.5 Правило очистки постмортема — в шаблоне и в `SKILL.md`

## 5. Документация скилла

- [x] 5.1 Описать в `references/kb-format.md` виды маскирования и форму меток
- [x] 5.2 Описать перечень немаскируемого и причину, по которой он существует
- [x] 5.3 Уточнить раздел «Границы» в `SKILL.md`: что вычищается и что очистка не отменяет
      правила про шаблоны вместо сырых дампов
- [x] 5.4 Указать в `templates/incident.md` и `templates/postmortem.md`, что текст проходит очистку

## 6. Проверки

- [x] 6.1 Фикстура с заведомо поддельными персональными данными и секретами
- [x] 6.2 Проверить маскирование каждого вида
- [x] 6.3 Проверить отсутствие маскирования для каждой позиции перечня немаскируемого
- [x] 6.4 Проверить, что шестнадцатизначный идентификатор, не проходящий контрольную сумму,
      не принимается за карту
- [x] 6.5 Проверить стабильность: одно значение — одна метка в пределах записи
- [x] 6.6 Регрессия: `Authorization: Bearer <токен>` — токен не попадает в запись
- [x] 6.7 Проверить совпадение результата на пути в базу знаний и на пути в текст тикета

## 7. Существующая база

- [x] 7.1 Прогнать проверку по имеющимся записям и посмотреть, есть ли срабатывания
      (прогнано по `tests/fixtures/kb/*.md` и `incident-detective/kb/README.md` —
      других записей базы знаний в репозитории нет; срабатываний не найдено)
- [x] 7.2 Правки существующих записей — вручную и осознанно, без массовой автоматической
      перезаписи (правок не потребовалось: срабатываний по 7.1 не было)
- [x] 7.3 Прогнать `openspec validate` по изменению
