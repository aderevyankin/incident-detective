## 1. parse_logs.py: корректность подсчёта и склейки

- [ ] 1.1 Регресс-тест: `ValueError: bad input` в тексте записи даёт счётчик класса 1,
      а не 2; затем объединение находок `EXC_RE`/`PY_EXC_RE` в множество до инкремента
- [ ] 1.2 Регресс-тест: строка продолжения стектрейса, отбитая пробелами и содержащая
      таймстемп, не открывает новую запись; затем замена `stripped[0]` на проверку
      исходного `text[0]` и пропуск поиска таймстемпа для продолжений
- [ ] 1.3 Прогон всех фикстур: изменения эталонов только там, где прежний ответ
      фиксировал удвоенные частоты, каждое — с объяснением в expected-файле

## 2. parse_logs.py: краевые случаи входа

- [ ] 2.1 Битый `.xz`: тест на предупреждение без трассировки; добавить `lzma.LZMAError`
      (и проверить `bz2`) в перехват ошибок распаковки
- [ ] 2.2 Ротация: тест, что `app.log.1` и `messages.0` подхватываются при обходе
      каталога; расширить правило отбора файлов
- [ ] 2.3 Числовые уровни: тест `{"level": 40}` → ERROR; соответствие 10..50 в
      `norm_level`
- [ ] 2.4 Feb 29 без года: тест при невисокосном `INCIDENT_NOW` — время не теряется,
      берётся ближайший високосный год позади, допущение названо в сводке

## 3. parse_logs.py: вывод

- [ ] 3.1 JSON не усекается `--top`: тест, что при 15 группах и `--top 10` в JSON все
      15; убрать применение `top` из `render_json`
- [ ] 3.2 Усечение md-сводки сохраняет ранние шаблоны: тест с редким ранним шаблоном,
      переживающим усечение; правило усечения учитывает время первого появления
- [ ] 3.3 Сводка при `--level` оговаривает, что распределение построено после фильтра
      (проверить существующую оговорку тестом)

## 4. timeline.py и code_hints.py: время git

- [ ] 4.1 Тест с подменённой TZ окружения: время коммита в ленте не зависит от
      таймзоны машины; перевод получения коммитов на UTC-стабильный формат даты
- [ ] 4.2 Границы окна `commits_in_window` передаются git в той же шкале, что и лента;
      тест на окно, ловящее коммит на границе при смещённой TZ
- [ ] 4.3 Отсутствующий файл `--parsed`: понятная ошибка вместо трассировки, тест

## 5. Спека и приёмка

- [ ] 5.1 `npx @fission-ai/openspec@latest validate --specs --strict` — дельта
      log-parsing валидна
- [ ] 5.2 `python3 tests/run.py` — весь набор зелёный
- [ ] 5.3 `python3 tools/bench.py` — бюджеты не нарушены (правка склейки затрагивает
      горячий путь)
