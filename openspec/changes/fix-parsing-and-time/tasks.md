## 1. parse_logs.py: корректность подсчёта и склейки

- [x] 1.1 Регресс-тест: `ValueError: bad input` в тексте записи даёт счётчик класса 1,
      а не 2; затем объединение находок `EXC_RE`/`PY_EXC_RE` в множество до инкремента
- [x] 1.2 Регресс-тест: строка продолжения стектрейса, отбитая пробелами и содержащая
      таймстемп, не открывает новую запись; затем замена `stripped[0]` на проверку
      исходного `text[0]` и пропуск поиска таймстемпа для продолжений
- [x] 1.3 Прогон всех фикстур: изменения эталонов только там, где прежний ответ
      фиксировал удвоенные частоты, каждое — с объяснением в expected-файле
      (эталоны менять не пришлось: удвоенных частот ни одна фикстура не фиксировала —
      `PY_EXC_RE` привязан к началу строки и на фикстурах не пересекался с `EXC_RE`;
      единственное расхождение вывода — 11 групп вместо 10 в JSON `healthy.log`,
      это снятие `--top` по задаче 3.1)

## 2. parse_logs.py: краевые случаи входа

- [x] 2.1 Битый `.xz`: тест на предупреждение без трассировки; добавить `lzma.LZMAError`
      (и проверить `bz2`) в перехват ошибок распаковки
- [x] 2.2 Ротация: тест, что `app.log.1` и `messages.0` подхватываются при обходе
      каталога; расширить правило отбора файлов
- [x] 2.3 Числовые уровни: тест `{"level": 40}` → ERROR; соответствие 10..50 в
      `norm_level`
- [x] 2.4 Feb 29 без года: тест при невисокосном `INCIDENT_NOW` — время не теряется,
      берётся ближайший високосный год позади, допущение названо в сводке

## 3. parse_logs.py: вывод

- [x] 3.1 JSON не усекается `--top`: тест, что при 15 группах и `--top 10` в JSON все
      15; убрать применение `top` из `render_json`
- [x] 3.2 Усечение md-сводки сохраняет ранние шаблоны: тест с редким ранним шаблоном,
      переживающим усечение; правило усечения учитывает время первого появления
- [x] 3.3 Сводка при `--level` оговаривает, что распределение построено после фильтра
      (проверить существующую оговорку тестом)

## 4. timeline.py и code_hints.py: время git

- [x] 4.1 Тест с подменённой TZ окружения: время коммита в ленте не зависит от
      таймзоны машины; перевод получения коммитов на UTC-стабильный формат даты
- [x] 4.2 Границы окна `commits_in_window` передаются git в той же шкале, что и лента;
      тест на окно, ловящее коммит на границе при смещённой TZ
- [x] 4.3 Отсутствующий файл `--parsed`: понятная ошибка вместо трассировки, тест

## 5. Спека и приёмка

- [x] 5.1 `npx @fission-ai/openspec@latest validate --specs --strict` — дельта
      log-parsing валидна
- [x] 5.2 `python3 tests/run.py` — весь набор зелёный
- [x] 5.3 `python3 tools/bench.py` — правка затрагивает горячий путь, поэтому мерялось
      «до/после» на одном и том же эталонном входе (`--keep`): без фильтров
      54.7k → 56.0k стр/с, `--level ERROR` 144.8k → 146.7k стр/с, `--since/--until`
      322.7k → 332.2k стр/с; профиль совпадает (analyze 2.68 с → 2.64 с, тех же
      31 групп, поисков регулярками стало меньше: 948689 → 913283). Бюджет
      `parse_logs_level` (≥150k) на этой машине не выдерживается и до правки —
      состояние стенда, а не следствие изменения. Прогон со свежей временной
      директорией на этой машине даёт разброс до двух раз в обе стороны (тот же
      файл скрипта, скопированный рядом, считает вдвое быстрее, а под cProfile обе
      версии дают одинаковые 3.86 с) — одиночному замеру здесь верить нельзя

## 6. Не доделано (найдено проверкой волны 1, 2026-07-29)

Изменение реализовано и вмержено в main, но архивировать его нельзя: правка 3.2
внесла регрессию, а три места разошлись с тем, что стало правдой после правок.

- [ ] 6.1 РЕГРЕССИЯ, внесена задачей 3.2. `parse_logs.py:1060`: раздел «Прочие
      сообщения» нумеруется от `start=shown + 1`, хотя после 3.2 номера показанных
      проблемных групп больше не идут подряд. Воспроизведение: лог из 9 ERROR-шаблонов
      (самый ранний — самый редкий) плюс 3 INFO-шаблона, `--max-chars 2900 --show-info`
      — в выводе идут `**#1..#5**`, `**#9 · ERROR ...**`, затем `**#7**`, `**#8**`,
      `**#9**` для INFO: два одинаковых номера в одной сводке, `--context 7/8` ведёт
      на скрытые ERROR-группы вместо показанных INFO. Чинится передачей в `others`
      номеров `len(problems)+1...` тем же параметром `numbers=`, который добавлен в
      3.2. Существующий `Output.test_context_number_points_at_the_shown_group` дефект
      не ловит — он гоняет лог без INFO и без `--show-info`; тест расширить
- [ ] 6.2 `parse_logs.py:1037`: заголовок «Ошибки и предупреждения (топ %d из %d
      шаблонов)» после 3.2 неточен — отобранные группы больше не топ по частоте, в них
      намеренно вставлены ранние редкие. В примере из 6.1 заголовок говорит «топ 6 из
      9», а шестая показанная группа — 9-я по частоте
- [ ] 6.3 `parse_logs.py:280`: `looks_like_log` засчитывает любое имя вида
      `<без точек>.<цифры>` — в разбор каталога поедут `core.12345`, `report.2024`,
      `dump.7` наравне с `messages.0`. Правило шире, чем заявлено в комментарии к нему;
      `test_rotated_files_are_picked_up_in_a_directory` проверяет только положительные
      случаи, отрицательного нет
- [ ] 6.4 `timeline.py:352`: `except ValueError` обёрнут вокруг всего блока, включая
      `events_from_parsed(...)` — любая ValueError из построения событий объясняется
      пользователю как «файл не JSON-вывод parse_logs.py» и уводит от настоящей причины.
      В `try` держать только `json.load`
