## 1. Перекомпоновка презентации

- [x] 1.1 Удалить слайды «Инцидент на стенде» и «Почему разбор дорог»; первый
      содержательный слайд — одна фраза о том, что делает скилл и что получает
      пользователь
- [x] 1.2 Слайд «Как проходит разбор»: нумерованная цепочка план → карточка →
      источники → разбор одной командой (логи → база → код → уверенность) → чтение
      кода → вывод → пополнение базы → следующий шаг; сверить названия шагов с
      планом работ шага 0 `incident-detective/SKILL.md`
- [x] 1.3 Пометить в цепочке точки решения (предпроверка, карта источников, уровень
      уверенности) и назвать управление порядком — бюджет пяти вызовов из
      существующего факта `facts`, без новых чисел
- [x] 1.4 Переакцентировать слайд уверенности: свойство «почему выводу можно верить»
      с порогами 0.70/0.40; поднять слайд «Чем отличается от вопроса модели напрямую»
      сразу за ним
- [x] 1.5 Слайд «Что ещё внутри»: база знаний, карта источников, ограниченный режим,
      телеметрия — каждое со смыслом для пользователя, не списком умений
- [x] 1.6 Слить «Скорость в цифрах» и «Что скилл не делает» в слайд свойств и границ;
      числа и подписи источников сохранить без изменений

## 2. Пример AIOps

- [x] 2.1 Слайд автономного сценария: вход (алерт), запуск (команда в изолированной
      среде), выход (`report.json` — вердикт, уверенность, доказательства, черновик
      шага), условие применения; без чисел, не подтверждаемых запуском в репозитории
- [x] 2.2 По готовности сквозного прогона `add-aiops-mode` (его задача 7.4) наполнить
      слайд реальной командой и фрагментом отчёта; отметить в `add-aiops-mode`, что
      его задача 6.4 исполнена этим изменением

## 3. Установка — только GigaCode

- [x] 3.1 Слайд установки: команды `cp -R` в `~/.gigacode/skills/` и
      `.gigacode/skills/`, перезапуск CLI, `/skills`; таблицу платформ со слайда
      убрать
- [x] 3.2 README «Быстрый старт» и пример пути `$SKILL_DIR`
      (`~/.qwen/skills/incident-detective`) — на `.gigacode`; таблица платформ README
      не меняется, строка Qwen Code на месте
- [x] 3.3 Проверить `docs/walkthrough.md` и `AI_CONTEXT.md`: `.qwen`-пути только в
      примерах установки переводятся; объяснение кальки платформ и оснастка
      разработки (`.qwen/commands/`, `.claude/`) не трогаются

## 4. Приёмка

- [x] 4.1 Слайды проверены в обеих темах и при печати: страница на слайд, без
      внешних ресурсов, воркфлоу-слайд в один экран
- [x] 4.2 `python3 tools/check_docs.py` — факты и команды блоков сходятся; если
      проверки привязаны к именам удалённых слайдов — поправить имена, не ослабляя
      сверку фактов
- [x] 4.3 `python3 tests/run.py` — общий прогон зелёный
- [x] 4.4 `npx @fission-ai/openspec@latest validate --specs --strict` — дельты
      project-presentation и project-documentation валидны

## 5. Не доделано (найдено проверкой волны 1, 2026-07-29)

Изменение реализовано и вмержено в main, но пункт приёмки 4.1 не выполнен, а презентация
противоречит сама себе по числам.

- [ ] 5.1 ЧИСЛА РАСХОДЯТСЯ. `docs/presentation.html:265` (слайд уверенности) и слайд
      «Пример 1» показывают «Уверенность в выводе: 0.33 — гипотеза» и «Код 0.05», а
      соседний новый слайд «Пример 2. Тот же инцидент» — 0.39. Прогон ровно той команды,
      что напечатана на слайде 1 (`triage.py tests/fixtures/logs/payment.log --kb
      tests/fixtures/kb --repo tests/fixtures --stand stage --service payment-api
      --query "connection timeout"`) даёт 0.39 и вклад контура «Код» 0.25, а не 0.05;
      `docs/walkthrough.md:138` тоже 0.39. Число 0.33 досталось от main, но слайд
      переписан этим изменением — правится здесь. Перепроверить прогоном ВСЕ числа
      диалогового примера, а не только AIOps-слайд
- [ ] 5.2 ПУНКТ 4.1 НЕ ВЫПОЛНЕН на 1024×768 (обычный проектор): слайд «Свойства и
      границы» прокручивается по вертикали на 20 px, `<pre>` на «Примере 2» — по
      горизонтали на 27 px, `<pre>` на «Установке» — на 3 px. Версия main на том же
      разрешении не переполняется ни одним слайдом. 1280×800 и 1122×793 действительно
      чистые — проверять надо и узкий экран
- [ ] 5.3 `docs/presentation.html:120-126`: комментарий «Цепочка шагов разбора: девять
      пунктов…» стоит над правилом `.code-dense pre`, а правила `.chain` идут ниже
      второго комментария — комментарии разъехались со своими правилами
- [ ] 5.4 РАСХОЖДЕНИЕ СПЕКИ С design.md. `specs/project-presentation/spec.md` дописан
      требованием «витринная копия таблицы SHALL оставаться в презентации», тогда как
      design.md в Non-Goals говорит «слайд 3 пересказывает смысл, не копируя таблицу» и
      называет целевой состав в девять слайдов; фактически слайд «Три контура и
      сигнатура» с копией таблицы оставлен, слайдов 11. Спека теперь предрешает вопрос,
      отнесённый к зоне `dedupe-canons`. Решить, что верно, и привести design.md, спеку
      и презентацию к одному ответу — до архивации, иначе `dedupe-canons` получит
      противоречивое требование
- [ ] 5.5 Правка сделана вне Impact: `openspec/changes/archive/2026-07-28-add-aiops-mode/
      tasks.md` (отметка об исполнении его задачи 6.4). По существу предписана задачей
      2.2, но файл лежит в архиве закрытого изменения — упомянуть в Impact либо снять
- [ ] 5.6 Задача 3.1 «таблицу платформ со слайда убрать» отмечена [x], но убирать было
      нечего: в main на слайде установки таблицы платформ не было вовсе
      (`git show <pre-wave>:docs/presentation.html`). Галочка не соответствует правке —
      переформулировать задачу или снять отметку
