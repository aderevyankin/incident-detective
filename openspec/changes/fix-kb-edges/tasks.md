## 1. kb_add.py: счётчик переиспользования

- [x] 1.1 Регресс-тест: `--update INC-x --outcome refuted` не меняет `reuse_count`,
      `reused_at` и не пишет «Повторилось» в «Заметки»; затем условие инкремента —
      только при свидетельстве повтора (`--signature`/`--from-parsed`/`--stand`/текст
      разделов разбора)
- [x] 1.2 Тест: `--update` со `--stand`/`--from-parsed` по-прежнему инкрементирует
      счётчик и дату; автоподхват по сигнатуре в автономном режиме — тоже

## 2. kb_add.py: очистка списочных полей

- [x] 2.1 Регресс-тест: секрет в `--tags` и e-mail в `--stand` маскируются во
      фронтматтере; затем прогон `--tags`/`--stand` через `scrub` с общим
      `scrub_counts` в ветках создания и обновления
- [x] 2.2 Тест: имя сервиса из `--from-parsed` с токеном маскируется; сводка очистки
      за запуск учитывает срабатывания на этих полях

## 3. kb_common.py: обязательные поля фронтматтера

- [x] 3.1 Регресс-тест: запись без `--stand`/`--tags` содержит `stands: []` и
      `tags: []`; затем в `dump_frontmatter` перечень полей, не выпадающих пустыми
- [x] 3.2 Тест: необязательные списки (`files`, `commits`, `related`, `signatures`,
      `services`) пустыми по-прежнему опускаются; чтение старой записи без
      `stands`/`tags` даёт пустые списки без ошибки
- [x] 3.3 Прогон фикстур записи: эталоны меняются только там, где фиксировали
      отсутствие обязательных полей, каждое изменение — с объяснением

## 4. confidence.py: подсказка контура базы знаний

- [x] 4.1 Регресс-тест: найденная запись с непроверенным исходом и чужим стендом
      (value < 0.5) — в «Что поднимет уверенность» нет текста «похожего случая в базе
      нет»; затем `score_kb` отдаёт причинную подсказку (`hint`), рендер предпочитает
      её статическому `WEAK_HINT['kb']`
- [x] 4.2 Тест: пустая выдача базы — прежняя подсказка «запиши этот случай»;
      непроверенный исход — «подтверди исход записи <id>»; чужой стенд — «сверь
      условия»; JSON-вывод расширен совместимо

## 5. Спека и приёмка

- [x] 5.1 `npx @fission-ai/openspec@latest validate --specs --strict` — дельты
      knowledge-base и confidence-scoring валидны
- [x] 5.2 `python3 tests/run.py` — весь набор зелёный

## 6. Не доделано (найдено проверкой волны 1, 2026-07-29)

Изменение реализовано и вмержено в main, но задача 2 закрыта не полностью: собственная
дельта спеки требует, чтобы очистка охватывала все поля записи, а один путь её обходит.

- [x] 6.1 УТЕЧКА СЕКРЕТОВ, задача 2 не доведена. `kb_add.py` (add_source, строки
      ~219-220 и ~312): путь записи карты источников (`--kind source`) не прогоняет
      `--stand` и `--service` через `scrub`. Воспроизведено дважды, в том числе вручную:
      `kb_add.py --kb <dir> --kind source --stand 'stage-ivan@example.com'
      --service 'payment-api?token=abc123secret' --source kind=mcp` создаёт файл
      `SRC-stage-ivan-example-com-payment-api-token-abc123.md` с `stand:
      stage-ivan@example.com` и `services: [payment-api?token=abc123secret]` — секрет
      попадает не только во фронтматтер, но и в id, и в имя файла. Соседние поля той же
      функции (`source`, `address`, `fields`, `query`, строки ~261-264) уже чистятся,
      так что объяснение «в карте источников адреса и запросы легитимны» на стенд и
      сервис не распространяется. Сценарий дельты «Очистка охватывает списочные поля
      фронтматтера» называет `--stand` без оговорки про вид записи
- [ ] 6.2 `confidence.py`: новая подсказка подставляет метку контура в неверном падеже —
      «сверь условия: запись INC-2026-01-003 с другого стенд (stage)», для сервиса
      «с другого сервис». Рядом лежащее предупреждение построено грамотно («в записи X
      другой стенд (stage)»). Тест проверяет только подстроку «сверь условия» и падежа
      не ловит
