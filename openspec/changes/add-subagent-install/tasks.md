## 1. Определение субагента

- [ ] 1.1 Создать `agents/incident-detective-log-parser.md`: фронтматтер `name`,
      `description` (источник вызова — скилл, ограничение применения), тело с контрактом —
      вход (пути к логам, карточка инцидента), запуск скриптов `scripts/` установленного
      скилла, выход (сигнатуры, счётчики, окна времени, «данных нет»), запрет на возврат
      сырых дампов
- [ ] 1.2 Проверить фронтматтер на переносимость: только поля пересечения Qwen
      Code/Claude Code, имя строчными через дефис

## 2. Реестр платформ

- [ ] 2.1 Внести в `incident-detective/references/platforms.md` личные и проектные
      директории агентов каждой платформы; путь GigaCode пометить «непроверено» рядом с
      пометкой для скиллов

## 3. Скилл

- [ ] 3.1 Добавить в `SKILL.md` раздел о делегировании: когда делегировать (файлы,
      директории, архивы + агент установлен), когда разбирать в диалоге (одна вставка,
      агента нет, пользователь просит показать ход), что передавать и что ожидать в ответ
- [ ] 3.2 Зафиксировать, что результат субагента идёт по общему пути: поиск по базе по
      сигнатурам и скоринг уверенности без особых правил
- [ ] 3.3 При отсутствии агента — разбор в основном диалоге с упоминанием, что субагента
      можно поставить копированием файла

## 4. Документация и проверка

- [ ] 4.1 Обновить `README.md`: состав поставки (`agents/`), установка субагента вторым
      копированием рядом с установкой скилла — необязательный шаг, директории агентов по
      платформам, предостережение не перезаписывать чужой одноимённый файл не глядя
- [ ] 4.2 Проверить вручную на Qwen Code: файл скопирован в директорию агентов — скилл
      делегирует разбор, файла нет — разбор идёт в основном диалоге без ошибок
- [ ] 4.3 `openspec validate add-subagent-install --strict`
