## 1. Определение субагента

- [ ] 1.1 Создать `agents/incident-detective-log-parser.md`: фронтматтер `name`,
      `description` (источник вызова — скилл, ограничение применения), тело с контрактом —
      вход (пути к логам, карточка инцидента), запуск скриптов `scripts/` установленного
      скилла, выход (сигнатуры, счётчики, окна времени, «данных нет»), запрет на возврат
      сырых дампов
- [ ] 1.2 Проверить фронтматтер на переносимость: только поля пересечения Qwen
      Code/Claude Code, имя строчными через дефис

## 2. Установщик

- [ ] 2.1 Добавить в реестр платформ `install.sh` директории агентов (личные и проектные)
      для qwen, claude, gigacode; путь GigaCode пометить «непроверено» рядом с пометкой
      для скиллов
- [ ] 2.2 Добавить шаг копирования агента после успешной установки скилла: та же
      платформа, та же область; печать пути установленного агента
- [ ] 2.3 Реализовать правила перезаписи: одинаковое содержимое — молча пропустить,
      другое содержимое — подтверждение (в неинтерактивном режиме только с `--yes`)
- [ ] 2.4 Добавить флаг `--no-agents` в разбор аргументов и в справку; неизвестные
      аргументы по-прежнему отклоняются
- [ ] 2.5 Сделать неудачу копирования агента некритичной: скилл установлен, пользователю
      объяснено, что разбор логов пойдёт в основном диалоге

## 3. Скилл

- [ ] 3.1 Добавить в `SKILL.md` раздел о делегировании: когда делегировать (файлы,
      директории, архивы + агент установлен), когда разбирать в диалоге (одна вставка,
      агента нет, пользователь просит показать ход), что передавать и что ожидать в ответ
- [ ] 3.2 Зафиксировать, что результат субагента идёт по общему пути: поиск по базе по
      сигнатурам и скоринг уверенности без особых правил

## 4. Документация и проверка

- [ ] 4.1 Обновить `README.md`: состав поставки (`agents/`), установка субагента по
      умолчанию, `--no-agents`, ручная установка копированием файла, директории агентов
      всех платформ
- [ ] 4.2 Прогнать `install.sh` во временную директорию (HOME-песочница): агент
      установлен той же областью, повторный запуск молчит, `--no-agents` не трогает
      директории агентов, конфликт содержимого требует подтверждения
- [ ] 4.3 `openspec validate add-subagent-install --strict`
