## 1. Поиск по базе знаний: недостающие приёмы

- [ ] 1.1 SKILL.md: там, где описан поиск по базе, добавить абзац про
      `kb_search.py --exclude-stand` — когда применять (сигнатура найдена, стенд
      другой: проверить, не переехала ли регрессия между стендами) и как читать пустую
      выдачу («на других стендах не встречалась», а не «записей нет вообще»)
- [ ] 1.2 Документировать `kb_search.py --repo` — повод: найденная запись содержит
      `files`, репозиторий под рукой; результат подаётся как сведение к проверке, а не
      вывод об устаревании
- [ ] 1.3 references/kb-format.md: добавить в таблицу полей frontmatter строки `files`
      и `commits` (необязательные; пишутся `kb_add.py --file`/`--commit`), сверить
      формулировки со спекой knowledge-base и templates/incident.md

## 2. Парсинг: перечень флагов

- [ ] 2.1 references/parsing.md: дополнить перечень флагов `--show-info` и
      `--max-chars` с назначением каждого, сверив описания с `--help` parse_logs.py

## 3. Предпроверка: git есть, но каталог не репозиторий

- [ ] 3.1 references/limited-mode.md: расширить каноническую команду предпроверки
      проверкой «каталог — git-репозиторий» (`git rev-parse --is-inside-work-tree`)
      с внятным исходом в выводе; команда остаётся одной строкой в одном месте
- [ ] 3.2 SKILL.md шаг 1: добавить в таблицу исходов строку «git есть, каталог не
      репозиторий → контур кода без git-истории, остальное как обычно»;
      references/limited-mode.md «Что говорить пользователю» — формулировка исхода

## 4. Падение оркестратора при пригодном окружении

- [ ] 4.1 SKILL.md шаг 4: добавить ветку «triage.py упал аварийно при пригодном
      окружении» — разбор продолжается отдельными скриптами цепочки
      (`parse_logs` → `kb_search --from-parsed` → `code_hints` → `confidence`) тем же
      интерпретатором; ограниченный режим не объявляется; падение называется в ответе

## 5. Формулировки, разошедшиеся с кодом и реестром

- [ ] 5.1 kb/README.md: заменить «пока здесь нет ни одной INC-*.md» на признак из
      `kb_is_empty` — выбор считается несделанным, пока нет ни `INC-*.md`, ни `SRC-*.md`
- [ ] 5.2 references/platforms.md: добавить в реестр строку «встроенный инструмент
      тикетов» (нет на обеих платформах, с состоянием проверки);
      references/next-step.md: заменить платформенно-именованную фразу отсылкой к
      реестру, сохранив «четвёртого пути не существует»

## 6. Спеки и приёмка

- [ ] 6.1 `npx @fission-ai/openspec@latest validate --strict` — дельты runtime-preflight
      и skill-packaging валидны
- [ ] 6.2 `python3 tools/check_docs.py` — зелёный: перечни флагов и копии команды
      предпроверки не разъехались
- [ ] 6.3 `python3 tests/run.py` — весь набор зелёный (скрипты не менялись, прогон —
      страховка)
