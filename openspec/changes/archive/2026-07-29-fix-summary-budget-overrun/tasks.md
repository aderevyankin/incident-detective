## 1. Регресс-тесты до правки

- [x] 1.1 Снять `OVERHEAD = 1.1` в `tests/test_summary_budget.py`: ассерты
      `test_trace_summary_is_capped_without_the_flag` и
      `test_timeline_summary_is_capped_without_the_flag` сравнивают с
      `MAX_SUMMARY_CHARS` строго. Убедиться, что оба падают: 12 585 и 12 067 против
      12 000
- [x] 1.2 Тест, что укладка вообще действует, остаётся отдельным и не сливается со
      строгим ассертом: без `--max-chars` те же прогоны дают 74 513 и 16 659 символов —
      разница «предел работает» и «предел точен» должна читаться по разным падениям

## 2. Расчёт доступного объёма

- [x] 2.1 Прочитать оба вызова (`trace.py:345`, `timeline.py:273`) и выбрать способ
      из design: параметр `used` у `fit_by_render` либо вычитание длины буфера шапки в
      вызывающем коде. Решение записать в design как принятое
- [x] 2.2 `trace.py`: доступный объём считается от фактически записанного, константа
      `reserve=260` уходит; резерв остаётся только под хвост «не показано»
- [x] 2.3 `timeline.py`: то же, `reserve=320` уходит
- [x] 2.4 Проверить `parse_logs.py` на предмет того, что канон именно там: если способ
      расчёта различается по мелочам, свести к одному — иначе `dedupe-canons` получит
      три похожие реализации вместо одной

## 3. Спеки и приёмка

- [x] 3.1 Дельта `performance-budgets`: сценарий «в предел входит весь вывод скрипта»
- [x] 3.2 `npx @fission-ai/openspec@latest validate --strict` — дельта валидна
- [x] 3.3 `python3 tests/run.py` — весь набор зелёный, включая строгие ассерты из 1.1
- [x] 3.4 Замер после правки записать в design рядом с таблицей «до»: сколько символов
      дают те же две фикстуры
