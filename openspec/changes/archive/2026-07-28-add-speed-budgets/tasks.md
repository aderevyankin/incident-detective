## 0. Порядок работ

- [x] 0.1 Убедиться, что изменение `add-runtime-preflight` реализовано: предпроверка
      окружения и ограниченный режим должны существовать до правок `SKILL.md` здесь —
      оба изменения трогают шаги 1–7, и бюджет раундов считается с учётом предпроверки
- [x] 0.2 Исправить `openspec/config.yaml`: ключ правил называется `spec`, а CLI ожидает
      `specs` — сейчас правила спеков молча не применяются (`Unknown artifact ID in
      rules: "spec"` в выводе `openspec instructions`)

## 1. Бенчмарк — сначала измерение, потом оптимизация

- [x] 1.1 Написать `tools/bench.py`: детерминированная генерация эталонного лога
      (400 000 строк, фиксированное зерно, смесь текста, стектрейсов и trace id),
      только stdlib, без сети
- [x] 1.2 Добавить замер каждого скрипта и вывод таблицей: скрипт, измерено, бюджет,
      вердикт; ненулевой код возврата при нарушении бюджета
- [x] 1.3 Добавить в вывод пометку об окружении замера (версия Python, платформа)
- [x] 1.4 Зафиксировать базовый прогон на текущем коде как точку отсчёта

## 2. Отсев записей до полного разбора

- [x] 2.1 Добавить в `parse_logs.py` быстрый отсев по сырой строке для `--level`:
      только при однозначно распознанном уровне ниже требуемого
- [x] 2.2 Добавить отсев по сырой строке для окна `--since/--until` при однозначно
      распознанном времени вне окна
- [x] 2.3 Добавить отсев по `--grep` до полного разбора записи
- [x] 2.4 Исключить из отсева продолжения многострочных записей: строка после записи,
      прошедшей фильтр, разбирается всегда
- [x] 2.5 Проверить на эталонном входе, что при `--level ERROR` число найденных ошибок,
      состав шаблонов и сигнатуры совпадают с результатом до оптимизации
- [x] 2.6 Прогнать бенчмарк: пропускная способность с фильтром должна выйти на
      ≥ 150 000 строк/с

## 3. Горячий путь парсера

- [x] 3.1 ~~В `templatize()` обрезать сообщение до предельной длины **до** цепочки
      регулярных замен~~ — **отвергнуто на проверке**: маски сжимают текст в десятки раз,
      поэтому отрезанный хвост влияет на шаблон. На логе с длинными сообщениями 7 групп
      превратились в 10 и разъехались частоты. Обоснование — в design.md
- [x] 3.2 Убрать повторный вызов `find_timestamp()` на записи: передавать результат
      первого вызова дальше
- [x] 3.3 Сверить сводки до и после на эталонном входе — вывод должен совпадать
- [x] 3.4 Прогнать бенчмарк: без фильтров ≥ 50 000 строк/с
- [x] 3.5 Ранний выход из поиска таймстемпа при совпадении с нулевой позиции — главный
      источник времени: шесть шаблонов перебирались всегда
- [x] 3.6 Дешёвая проверка подстроки перед маской в `templatize()`
- [x] 3.7 Кэшировать сырой текст записи: `raw_text` склеивал список на каждое обращение

## 4. Бюджет объёма вывода

- [x] 4.1 Ввести в `parse_logs.py` предел объёма md-сводки (12 000 символов) с подгонкой
      числа показываемых шаблонов и сигнатур под него
- [x] 4.2 Добавить сообщение об усечении: сколько элементов не показано и как получить
      полный результат
- [x] 4.3 Применить тот же предел к выводу `code_hints.py` и `kb_search.py`
- [x] 4.4 Убедиться, что на `--format json` предел не распространяется
- [x] 4.5 Проверить на входе с большим разнообразием шаблонов, что сводка укладывается
      в бюджет и усечение объявляется

## 5. Бюджет обращений к git

- [x] 5.1 В `code_hints.py` запрашивать `git blame` только по фреймам, попавшим в вывод
- [x] 5.2 Кэшировать историю по файлу в пределах запуска: несколько фреймов одного файла
      дают один запрос
- [x] 5.3 Проверить на стектрейсе из двух десятков фреймов, что число запусков процессов
      `git` укладывается в бюджет
- [x] 5.4 Проверить, что при отсутствии `git` процессы не запускаются вовсе

## 6. Поиск по базе через индекс

- [x] 6.1 В `kb_search.py`/`kb_common.py` считать релевантность по `index.json`, читая
      markdown только для записей, попавших в выдачу
- [x] 6.2 Определить признак устарелости индекса (несоответствие набору файлов базы)
      и падать обратно на чтение markdown с сообщением о пересборке
- [x] 6.3 Проверить на базе из ~1000 сгенерированных записей: поиск ≤ 1 с, выдача
      совпадает с выдачей при чтении markdown

## 7. Оркестратор `triage.py`

- [x] 7.1 Написать `triage.py`: импорт соседних модулей, цепочка parse → kb → code →
      confidence в одном процессе, только stdlib, Python 3.8+
- [x] 7.2 Реализовать единую сводку с разделами по контурам и итоговым уровнем
      уверенности, в пределах бюджета объёма
- [x] 7.3 Сохранять машинные результаты этапов в JSON-файлы и называть пути в выводе
- [x] 7.4 Обработать неполные входные данные: нет репозитория, пустая база, нет
      сигнатур — этап помечается непройденным, остальные выполняются
- [x] 7.5 Пробросить в этапы флаги сужения: `--level`, `--since/--until`, `--grep`
- [x] 7.6 Проверить, что отдельные скрипты продолжают работать напрямую с прежними
      флагами
- [x] 7.7 Прогнать бенчмарк: цепочка целиком на эталонном входе ≤ 15 с
- [x] 7.8 Добавить `triage.py` в проверку совместимости `tools/check_compat.py`

## 8. Перестройка шагов SKILL.md

- [x] 8.1 Свести шаги 5, 6 и 7 (база по сигнатурам, код, уверенность) в один шаг —
      запуск оркестратора
- [x] 8.2 Объединить предпроверку окружения с первым содержательным вызовом, сохранив
      требование сообщить результат пользователю
- [x] 8.3 Записать правило сужения данных до разбора: окно и уровень задаются при
      выгрузке из источника и при запуске парсера, а не применяются к сводке
- [x] 8.4 Переписать стартовый список `todo_write` под объединённые шаги: задача на
      вызов инструмента, а не на контур; контуры перечислены в формулировке задачи
- [x] 8.5 Добавить раздел про прямой запуск отдельных скриптов для ручного разбора,
      чтобы объединение не выглядело их отменой
- [x] 8.6 Проверить, что ограниченный режим из `add-runtime-preflight` описан
      согласованно: оркестратор в нём не запускается
- [x] 8.7 Пересчитать по получившемуся тексту число вызовов инструментов в типовом
      разборе — должно уложиться в шесть

## 9. Документация и проверка

- [x] 9.1 Обновить `README.md`: `triage.py` в составе скриптов, раздел про скорость
      и бюджеты, `tools/bench.py` в разделе про проверки
- [x] 9.2 Убедиться, что `install.sh` не копирует `tools/` в директорию скилла
- [x] 9.3 Прогнать `tools/check_compat.py` и `tools/bench.py` на итоговом коде
- [x] 9.4 Прогнать `openspec validate add-speed-budgets --strict`
- [x] 9.5 Провести разбор на эталонном логе от начала до вывода и посчитать
      фактическое число вызовов инструментов
