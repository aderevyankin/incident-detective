## 1. Якорь пополнения базы в сводке разбора

- [x] 1.1 В `incident-detective/scripts/triage.py`, в хвосте сводки, добавить напоминание
      о шаге 8: записать разбор через `kb_add.py` с ключевыми флагами, а при отказе от
      записи назвать причину
- [x] 1.2 Оговорить в напоминании, что уровень уверенности его не отменяет: гипотеза
      записывается с исходом «не проверена»
- [x] 1.3 В автономном режиме напоминание заменить указанием на `kb_add.py --report` —
      существующую строку про отчёт сохранить, дубля не плодить
- [x] 1.4 Проверить, что напоминание не выходит за бюджет объёма сводки
      (`performance-budgets`, проверка в `tests/`)

## 2. Формулировка подсказки в контуре кода

- [x] 2.1 В `incident-detective/scripts/code_hints.py`, раздел «Что дальше», переписать
      строку «Правки предлагай только после того, как причина подтверждена логами» так,
      чтобы она различала готовый патч и названное направление фикса
- [x] 2.2 Убедиться, что новая формулировка верна при любом уровне уверенности: скрипт
      считает только свой контур и уровня разбора не знает

## 3. Шаги 6 и 7 SKILL.md

- [x] 3.1 В шаге 6 задать, что такое конкретное предложение фикса: файл и место, суть
      изменения, способ убедиться, что помогло
- [x] 3.2 В шаге 6 назвать, что конкретным предложением не считается: инструмент без
      предмета, «проверить код», «переписать метод»
- [x] 3.3 В шаге 7, пункт «Что делать», задать форму запроса данных: конкретное поле,
      строка лога, источник и окно времени
- [x] 3.4 Связать шаги 6 и 7 с таблицей уровней шага 9: «вероятная причина» — фикс с
      оговоркой об уровне, «гипотеза» — запрос данных, остановка на анализе недопустима
- [x] 3.5 Проверить, что новые правила не конфликтуют с границей «правки не применяются
      без подтверждения» из раздела «Границы»

## 4. Проверка

- [x] 4.1 Прогнать `triage.py` на фикстуре и глазами убедиться, что напоминание видно в
      хвосте сводки и читается однозначно
- [x] 4.2 Прогнать `code_hints.py` на фикстуре и проверить раздел «Что дальше»
- [x] 4.3 Расширить `check_closing_order` в `tools/check_docs.py` проверкой новых
      инвариантов: напоминание в `triage.py`, отсутствие запретительной формулировки в
      `code_hints.py`, критерий конкретности в шаге 6
- [x] 4.4 Прогнать `python3 tests/run.py` и `python3 tools/check_docs.py` — без падений
- [x] 4.5 Прогнать разбор на GigaCode и проверить: разбор не останавливается на анализе,
      предложение конкретно, база пополнена либо пропуск объяснён — **передано
      пользователю**: из репозитория прогон невыполним
