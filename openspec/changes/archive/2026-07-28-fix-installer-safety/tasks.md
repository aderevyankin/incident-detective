## 1. Установщик

- [x] 1.1 Разбор аргументов циклом: `--project`, `--yes`, `--help`; неизвестный аргумент —
      ошибка со справкой и ненулевой код
- [x] 1.2 Проверка TTY перед `read`; без TTY и без `--yes` — понятное сообщение и выход
- [x] 1.3 Бэкап записей: `mktemp -d` только при найденных `INC-*.md`, путь печатается,
      при успешном восстановлении директория удаляется
- [x] 1.4 Сборка новой копии во временной директории на томе назначения, без
      `__pycache__`/`*.pyc`
- [x] 1.5 Порядок замены: копия готова → записи возвращены → старая установка убрана →
      переименование на место
- [x] 1.6 Один запуск `kb_index.py` вместо двух

## 2. Проверки

- [x] 2.1 Тест: установка в изолированный `HOME`, в результате есть `SKILL.md` и скрипты,
      нет `__pycache__`
- [x] 2.2 Тест: переустановка с `--yes` поверх установки с подложенной записью `INC-*.md`
      — запись на месте, индекс пересобран
- [x] 2.3 Тест: запуск без TTY и без `--yes` завершается ненулевым кодом с объяснением
- [x] 2.4 Удалить `test_installer_copies_only_the_skill` (проверку текста скрипта)
- [x] 2.5 Прогнать `python3 tests/run.py`

## 3. Документация

- [x] 3.1 README: раздел установки — `--yes`, `--help`, поведение при переустановке и путь
      бэкапа
- [x] 3.2 Прогнать `openspec validate` по изменению
