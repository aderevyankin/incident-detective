## 1. Триггеры самозапуска в SKILL.md

- [x] 1.1 Добавить в frontmatter `description` короткую безглагольную формулировку
      («ошибка на dev/test/stage/prod»), голые ключевые слова «инцидент», «проблема»,
      «баг», «баги», «ошибка», «логи» без контекста и ссылку на файл логов как отдельный
      вид триггера, не смешанный со ссылками на системы мониторинга
- [x] 1.2 В разделе «Когда включаться» добавить те же пункты отдельными строками: голое
      «у нас ошибка на <стенд>», голое ключевое слово без контекста («проблема», «баги»,
      «логи» и т.д.) и ссылка/файл/архив с логами

## 2. Отказ инструмента плана в шаге 0

- [x] 2.1 Добавить в шаг 0 ветку: инструмент ведения задач отказан платформой по правам
      (не переименован — недоступен вовсе) → скилл называет это одной строкой и ведёт
      план текстом в каждом ответе, тем же составом задач, по тем же правилам обновления

## 3. Реестр платформ

- [x] 3.1 В `references/platforms.md`, раздел 7, обновить строку GigaCode: заменить
      «CLI в среде разработки не было» на наблюдение первого прогона (29.07.2026) —
      самозапуск не сработал на короткой фразе, план вёлся текстом из-за отказа
      инструмента плана, запись в базу знаний сделана мимо `kb_add.py`; платформа
      остаётся непроверенной

## 4. Синхронизация со спеками

- [x] 4.1 Прогнать `python3 tools/check_docs.py`, чтобы убедиться, что документация не
      разошлась с репозиторием
- [x] 4.2 Прогнать `npx @fission-ai/openspec@latest validate --specs --strict`
