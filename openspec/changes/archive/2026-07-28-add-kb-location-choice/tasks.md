## 1. Разрешение пути в kb_common

- [x] 1.1 Научить `kb_dir()` возвращать не только путь, но и источник: флаг,
      `INCIDENT_KB_DIR` или умолчание — не сломав существующих вызовов
- [x] 1.2 Добавить признак «база пуста»: наличие `INC-*.md`, а не самой директории
- [x] 1.3 Добавить определение корня проекта через `git rev-parse --show-toplevel`
      с откатом на текущую директорию, когда репозитория нет
- [x] 1.4 Добавить шаг разрешения «база в корне репозитория, если она существует» между
      `INCIDENT_KB_DIR` и умолчанием; директорию на этом шаге не создавать
- [x] 1.5 Отразить новый шаг в возвращаемом источнике пути, чтобы вопрос не задавался,
      когда база проекта найдена
- [x] 1.6 Проверить, что при заданных `--kb` и `INCIDENT_KB_DIR` поведение прежнее и
      переменная перебивает базу в репозитории

## 2. Сигнал от kb_add о невыбранном расположении

- [x] 2.1 При источнике «умолчание» и пустой базе завершать `kb_add.py` понятным
      сообщением о том, что расположение не выбрано, с ненулевым кодом
- [x] 2.2 Включить в сообщение предлагаемый путь в проекте и путь внутри скилла,
      чтобы агент составил из них варианты вопроса
- [x] 2.3 Не создавать директорию базы при таком выходе
- [x] 2.4 Проверить, что при непустой базе внутри скилла запись идёт как раньше,
      без сигнала

## 3. Вопрос и запоминание выбора в SKILL.md

- [x] 3.1 Дополнить шаг пополнения базы: вопрос через `ask_user_question` с готовыми
      вариантами «в корне проекта» (`memory/knowledgebase/`), «в директории скилла»,
      свой путь свободным ответом
- [x] 3.2 Описать, что вопрос задаётся только при первой записи и не задаётся при
      поиске в начале разбора
- [x] 3.3 Описать вывод готовой строки `export INCIDENT_KB_DIR=<путь>` после выбора
- [x] 3.4 Описать, что для базы в корне проекта строка не обязательна: выбор читается по
      наличию директории, а `export` нужен для работы с этой базой из других директорий
- [x] 3.5 Описать предупреждение о git при выборе пути внутри репозитория — одной
      строкой, без уговоров
- [x] 3.6 Описать поведение при отказе выбирать: запись в базу скилла, повторно в
      этом разборе не спрашивать

## 4. Документация

- [x] 4.1 Дополнить `references/kb-format.md` разделом о расположении базы и способах
      его задать
- [x] 4.2 Дополнить `README.md`: выбор расположения, строка `export`, последствия
      выбора «в проект»
- [x] 4.3 Отметить в `README.md`, что база вне скилла переустановкой не затрагивается

## 5. Проверка

- [x] 5.1 Прогнать запись при пустой базе и незаданном пути — убедиться, что скрипт
      сигнализирует, а директория не создаётся
- [x] 5.2 Прогнать запись с `--kb` во временную директорию — убедиться, что вопроса
      нет и запись создаётся
- [x] 5.3 Прогнать запись с `INCIDENT_KB_DIR` — то же самое
- [x] 5.4 Прогнать запись при непустой базе внутри скилла — убедиться, что сигнала нет
- [x] 5.5 Прогнать определение корня проекта вне git-репозитория
- [x] 5.6 Прогнать запись в репозитории, где база в корне уже есть — убедиться, что вопроса
      нет и запись идёт в неё
- [x] 5.7 Прогнать тот же случай с заданной `INCIDENT_KB_DIR` — убедиться, что переменная
      перебивает базу репозитория
- [x] 5.8 Прогнать два репозитория со своими базами при закреплённой переменной от третьего
      пути — убедиться, что записи не смешиваются
- [x] 5.9 Убедиться, что путь, в который ушла запись, назван в ответе — особенно когда база
      найдена в корне репозитория, а не задана явно
- [x] 5.10 Прогнать `openspec validate` по изменению
