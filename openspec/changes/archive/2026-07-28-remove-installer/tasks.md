# remove-installer — tasks

## 1. Удаление установщика

- [x] 1.1 Удалить `install.sh`
- [x] 1.2 Удалить класс `Installer` из `tests/test_packaging.py`
- [x] 1.3 Добавить в `tests/test_packaging.py` проверку чистоты поставки: в списке файлов
      `incident-detective/` под версионным контролем нет `__pycache__` и `*.pyc`; при
      недоступном `git` — пропуск с пометкой, а не падение

## 2. README

- [x] 2.1 Переписать «Быстрый старт»: клонировать репозиторий или дать ссылку на него
      своему AI-ассистенту, скопировать `incident-detective/` в директорию скиллов
      платформы, перезапустить CLI; без жёсткого URL репозитория
- [x] 2.2 Убрать абзацы про флаги установщика, переустановку, резервные копии и
      неинтерактивный режим
- [x] 2.3 Добавить предупреждение об обновлении копированием: база знаний внутри
      директории скилла затирается; способы сохранить записи (внешнее расположение,
      ручное сохранение `INC-*.md`)
- [x] 2.4 Обновить таблицу «Поддерживаемые платформы»: колонка установки — копирование
      для всех платформ; убрать из абзаца под таблицей упоминание установщика
- [x] 2.5 Поправить раздел «База знаний» («База вне скилла переустановкой не
      затрагивается» ссылается на `install.sh`) и раздел «Требования к окружению»
      («установщик может и не запускаться»)

## 3. Сопутствующая документация

- [x] 3.1 `AI_CONTEXT.md`: убрать `install.sh` из раскладки и команд
- [x] 3.2 `docs/presentation.html`: слайд «Установка и что дальше» — установка
      копированием; убрать `install.sh` из `referenced-files`

## 4. Спеки и проверки

- [x] 4.1 Прогнать `python3 tests/run.py` — весь набор зелёный
- [x] 4.2 Прогнать `openspec validate --specs --strict` на дельтах изменения
- [x] 4.3 Убедиться, что `tools/check_docs.py` (в составе прогона) принимает новый README
      и презентацию
