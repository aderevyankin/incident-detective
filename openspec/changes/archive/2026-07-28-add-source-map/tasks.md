## 1. Формат записи и общий слой

- [x] 1.1 Ввести в `kb_common.py` понятие вида записи: константы `KIND_INCIDENT` /
      `KIND_SOURCE`, чтение поля `kind` из фронтматтера, отсутствие поля трактуется как
      `incident`
- [x] 1.2 Описать разбор и сборку фронтматтера записи карты: `stand`, `services`,
      `source`, `address`, `query`, `fields`, `confirmed`, `checked`
- [x] 1.3 Добавить константу срока старения записи карты (30 дней) и функцию расчёта
      возраста с признаком «требует проверки»
- [x] 1.4 Прогнать поля записи карты через существующую очистку секретов; убедиться, что
      маскирование строки подключения и токена в запросе работает
- [x] 1.5 Добавить шаблон записи карты в `incident-detective/templates/`

## 2. Индекс и поиск

- [x] 2.1 `kb_index.py`: сохранять вид записи в `index.json`, не ломая чтение старого
      индекса без этого поля
- [x] 2.2 `kb_search.py`: по умолчанию исключать записи карты из поиска по инцидентам
- [x] 2.3 `kb_search.py --sources --stand <s> --service <svc>`: выдача записей карты с
      источником, адресом, запросом, датой подтверждения и возрастом
- [x] 2.4 Помечать в выдаче устаревшую запись карты как требующую проверки
- [x] 2.5 Показывать в выдаче отвергнутые источники (`checked`) с причиной и датой
- [x] 2.6 `kb_search.py --signature <sig> --exclude-stand <s>`: cross-stand поиск с
      указанием стенда, даты и исхода у каждой записи; пустая выдача формулируется как
      «на других стендах не встречалась»

## 3. Запись и обновление карты

- [x] 3.1 `kb_add.py --kind source`: создание записи карты с обязательными стендом,
      сервисом и источником
- [x] 3.2 Обновление существующей записи карты через `--update <id>`: обновляется дата
      подтверждения, дубль не создаётся
- [x] 3.3 `kb_add.py --mark-checked <источник> --verdict empty|unavailable --note <текст>`:
      пометка бесполезного источника с датой по имени инструмента; повторная пометка
      обновляет запись
- [x] 3.4 Отклонять попытку сохранить в запись карты фрагмент выгруженных логов

## 4. Инструкции скиллу

- [x] 4.1 `references/log-sources.md`: зафиксировать порядок обследования — запрос
      пользователя, карта, приложенные данные, перебор остальных каналов; исключение
      помеченных источников из перебора
- [x] 4.2 `references/log-sources.md`: расширить перебор за пределы MCP — установленные
      скиллы, локальные файлы и каталоги логов проекта, веб-интерфейсы под учётными
      данными пользователя; конфигурации платформ читаются, но не правятся
- [x] 4.3 `references/log-sources.md`: развести осмотр состава (всегда) и обращение к
      источнику (по карте); что делать с инструментом, появившимся между разборами, и с
      отвалившимся источником из карты
- [x] 4.4 `references/log-sources.md`: работа со ссылкой — карта, затем попытка агента,
      затем просьба о выгрузке с готовым запросом и окном; скрипты в сеть не ходят
- [x] 4.5 `references/log-sources.md`: раздел о пополнении карты сразу после успешной
      выгрузки, до парсинга
- [x] 4.6 `references/kb-format.md`: формат записи карты и её отличие от разбора
- [x] 4.7 `SKILL.md`: пункты обследования каналов попадают в план работ, пропущенные
      отмечаются с причиной
- [x] 4.8 `SKILL.md`: шаг получения логов ссылается на новый порядок; проверить, что
      бюджет в пять вызовов инструментов до вывода причины сохраняется
- [x] 4.9 `SKILL.md`: не задавать вопрос о выборе источника, когда карта его уже называет

## 5. Тесты

- [x] 5.1 Запись карты создаётся, читается и обновляется; дубль не появляется
- [x] 5.2 База со старыми записями без `kind` читается, записи считаются инцидентами
- [x] 5.3 Поиск по инцидентам не возвращает записи карты, запрос карты не возвращает
      разборы
- [x] 5.4 Устаревшая запись помечается, свежая — нет
- [x] 5.5 Пометка бесполезного источника пишется, читается и обновляется по повторной
      проверке
- [x] 5.6 Секрет в запросе и в строке подключения маскируется перед записью
- [x] 5.7 Cross-stand поиск возвращает записи других стендов и отличает пустую выдачу от
      отсутствия записей вообще
- [x] 5.8 Поиск с записями карты в базе укладывается в бюджет времени
      (`tools/bench.py`)
- [x] 5.9 Результат по каждому каналу пополняет карту, включая пустой: «локальных логов
      нет», «скиллов для логов не установлено»

## 6. Документация

- [x] 6.1 `README.md`: карта источников в описании возможностей — что скилл запоминает
      между разборами; обновить описание базы знаний, где сейчас сказано «по файлу на
      инцидент»
- [x] 6.2 `AI_CONTEXT.md`: новый вид записи в разделе про базу знаний
- [x] 6.3 `docs/presentation.html`: карта источников в разделе возможностей; проверить,
      что описание базы знаний и порядка получения логов не разошлось с реализацией
- [x] 6.4 `docs/walkthrough.md`: сценарий второго разбора на том же стенде — без
      повторной инвентаризации
