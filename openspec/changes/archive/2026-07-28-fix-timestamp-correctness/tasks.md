## 1. Разбор смещения таймзоны в парсере

- [x] 1.1 Дополнить регулярки ISO и подобных форматов захватом суффикса зоны: `Z`, `+HH:MM`,
      `+HHMM`, `+HH`, `-HH:MM`, `-HHMM`, `-HH`
- [x] 1.2 Привести время с известным смещением к UTC при разборе, сохранив признак «зона известна»
- [x] 1.3 Оставить время без зоны наивным и пометить признаком «зона неизвестна»
- [x] 1.4 Заменить `datetime.fromtimestamp` на разбор epoch в UTC
- [x] 1.5 Проверить, что суффикс зоны не остаётся в тексте сообщения и не попадает в шаблон

## 2. Год для syslog

- [x] 2.1 Выбирать год по правилу «событие не в будущем»: месяц больше текущего — предыдущий год
- [x] 2.2 Зафиксировать подставленный год как допущение, доступное для вывода в сводке
- [x] 2.3 Проверить на записи с декабрьской датой при разборе в январе

## 3. Сводка и JSON-вывод парсера

- [x] 3.1 Указывать в сводке шкалу времени, в которой показаны все времена
- [x] 3.2 Показывать допущения: подставленный год, записи без зоны и их долю
- [x] 3.3 Добавить соответствующие поля в JSON-вывод, не меняя смысл существующих
- [x] 3.4 Проверить, что старый JSON-файл без новых полей читается как «зона неизвестна»

## 4. Общий модуль работы со временем

- [x] 4.1 Вынести в общий модуль: получение ключа сортировки, оценку расхождения часов,
      применение явной поправки
- [x] 4.2 Перевести `trace.py` на общий модуль, сохранив поведение `--check-clocks` и `--offset`
- [x] 4.3 Убедиться, что `parse_logs.py` остаётся без импортов модулей скилла

## 5. Хронология

- [x] 5.1 Нормализовать время всех источников до сортировки
- [x] 5.2 Сделать ключ сортировки устойчивым: время, источник, порядковый номер записи
- [x] 5.3 Добавить оценку расхождения часов между источниками с выводом предупреждения
- [x] 5.4 Добавить применение явно заданной поправки по источнику, без автоматической коррекции
- [x] 5.5 Указывать шкалу времени в выводе ленты

## 6. Смешанная лента

- [x] 6.1 Определять попадание в одну ленту записей с зоной и без неё
- [x] 6.2 Выводить предупреждение с последствием: порядок между группами может быть неверен
- [x] 6.3 Продублировать предупреждение в выводе цепочки запроса

## 7. Документация скилла

- [x] 7.1 Описать в `references/parsing.md` работу с зонами, допущения и их отображение
- [x] 7.2 Описать в `references/correlation.md` единый порядок действий при разъехавшихся часах
- [x] 7.3 Дополнить шаги 3.3 и 3.4 в `SKILL.md` упоминанием шкалы времени
- [x] 7.4 Проверить, что принцип «время автоматически не правится» остался в силе и в тексте

## 8. Проверка

- [x] 8.1 Лог с записями в `Z` и `+03:00` — убедиться, что порядок событий верный
- [x] 8.2 Лог с epoch и ISO из одного кластера — убедиться, что времена сходятся
- [x] 8.3 Смешанная лента с зоной и без — убедиться, что предупреждение выводится
- [x] 8.4 Декабрьский syslog при разборе в январе — убедиться, что год предыдущий
- [x] 8.5 Повторный запуск на одних данных — убедиться, что порядок совпадает до записи
- [x] 8.6 Прогнать `openspec validate` по изменению
