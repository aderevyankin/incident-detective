## 1. Маршрут завершения разбора

- [x] 1.1 В `incident-detective/SKILL.md`, шаг 7, пункт «Что делать»: убрать отсылку
      «отсюда разбор идёт в шаг 9» и заменить её переходом в шаг 8, назвав порядок
      завершения — вывод, запись в базу, предложение следующего шага
- [x] 1.2 Добавить в перечень пунктов вывода шага 7 строку «Запись в базу»: путь
      созданной или дополненной записи либо причина, по которой записи нет
- [x] 1.3 Дописать в шаг 7 абзац про проверку перед выводом: план ведётся, база
      пополнена или пропуск объяснён, три контура пройдены или непройденный назван с
      причиной — без второго списка-галочек в ответе
- [x] 1.4 В шаге 9 назвать условие входа: предыдущий шаг пройден либо отклонён с
      названной причиной
- [x] 1.5 Сверить `incident-detective/references/next-step.md` с новым маршрутом:
      формулировка «после пополнения базы знаний» должна совпадать с каноном в `SKILL.md`

## 2. Обязательность записи в базу

- [x] 2.1 Переписать вступление шага 8: шаг выполняется в каждом разборе, дошедшем до
      вывода; молчаливый пропуск запрещён
- [x] 2.2 Перечислить валидные причины пропуска: отказ пользователя, переиспользовать
      нечего, запись уже существует и дополнена, база недоступна
- [x] 2.3 Проверить, что правило не конфликтует с автономным режимом: там вопрос не
      задаётся, а судьба записи попадает в отчёт — при необходимости уточнить оговорку

## 3. Самозапуск

- [x] 3.1 Добавить в раздел «Когда включаться» пункт про прямую просьбу: упоминание
      скилла по имени, «используя скилл», «разбери скиллом», ссылка на его директорию
- [x] 3.2 Дописать там же запрет подменять запуск пересказом того, что скилл делает, или
      разбором без прохождения его шагов
- [x] 3.3 Добавить правило о первых действиях: признак инцидента распознан — план и
      предпроверка окружения до чтения логов; логи открываются через `parse_logs.py` или
      `triage.py`
- [x] 3.4 Оговорить границу: определить размер, формат, кодировку файла и распаковать
      архив можно до предпроверки; содержательный разбор прочитанных строк — нет
- [x] 3.5 Оговорить снятие запрета в ограниченном режиме со ссылкой на
      `references/limited-mode.md`
- [x] 3.6 Убедиться, что поле `description` во фронтматтере осталось нетронутым

## 4. Документация

- [x] 4.1 Сверить описание порядка шагов в `README.md` с новым маршрутом 7 → 8 → 9 и
      поправить расхождения
- [x] 4.2 Проверить `incident-detective/references/kb-format.md` и `onboarding.md` на
      формулировки, называющие пополнение базы опциональным

## 5. Проверка

- [x] 5.1 Добавить тест на инварианты текста `SKILL.md`: отсутствие маршрута из шага 7 в
      шаг 9 напрямую, наличие строки «Запись в базу», пункта про прямую просьбу и правила
      о первых действиях
- [x] 5.2 Прогнать `python3 tests/run.py` и `python3 tools/check_docs.py` — без падений
- [x] 5.3 Прогнать разбор того же инцидента (`DatabaseConnectionException`) на GigaCode и
      проверить: скилл запустился сам, план заведён, запись в базу создана либо пропуск
      объяснён — **передано пользователю**: прогон идёт в среде GigaCode после установки
      обновлённого скилла, из репозитория он невыполним
