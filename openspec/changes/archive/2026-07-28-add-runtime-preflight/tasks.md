## 1. Guard в скриптах

- [x] 1.1 Сформулировать блок проверки версии: до импортов модулей скилла, без f-строк,
      сообщение с требуемой (3.8) и найденной версией, выход с ненулевым кодом
- [x] 1.2 Добавить блок в запускаемые скрипты: `parse_logs.py`, `timeline.py`, `trace.py`,
      `code_hints.py`, `confidence.py`, `kb_search.py`, `kb_add.py`, `kb_index.py`
- [x] 1.3 Проверить, что блок не ломает `--help` и обычный запуск на пригодной версии
- [x] 1.4 Проверить сообщение вручную: запустить скрипт интерпретатором ниже 3.8 (или
      подменённым `sys.version_info`) и убедиться, что выводится сообщение, а не трассировка

## 2. Проверка совместимости с минимальной версией

- [x] 2.1 Добавить проверку, что все скрипты разбираются с `ast.parse(feature_version=(3,8))`
- [x] 2.2 Сделать её запускаемой одной командой, чтобы граница 3.8 не протухла при правках
- [x] 2.3 Прогнать проверку на текущем коде и зафиксировать результат

## 3. Предпроверка окружения

- [x] 3.1 Составить команду предпроверки: наличие и версия `python3`, наличие `git`,
      короткий однозначный вывод, работающий и при отсутствии интерпретатора
- [x] 3.2 Проверить команду в трёх состояниях: пригодный python3, версия ниже 3.8,
      python3 отсутствует
- [x] 3.3 Написать формулировки сообщений пользователю для каждого состояния — что найдено,
      что требуется, в каком режиме продолжится разбор

## 4. Ограниченный режим

- [x] 4.1 Описать порядок работы без скриптов: чтение `kb/INC-*.md`, точечные выборки по
      логам средствами оболочки, что именно не выполняется
- [x] 4.2 Описать пополнение базы знаний вручную по формату записи с пометкой, что индекс
      не пересобран
- [x] 4.3 Зафиксировать запреты: не подбирать другой интерпретатор, не писать свой парсер,
      не выдавать ручное чтение за результат контура
- [x] 4.4 Зафиксировать потолок «гипотезы» и обязательное указание режима в итоговом ответе
- [x] 4.5 Собрать это в новый файл `incident-triage/references/limited-mode.md`

## 5. SKILL.md

- [x] 5.1 Добавить шаг предпроверки окружения перед контурами, с командой и разбором исходов
- [x] 5.2 Добавить предпроверку первой задачей в стартовый список `todo_write` и правило
      перестроения плана при непригодном окружении
- [x] 5.3 Дополнить шаги логов, кода и уверенности отсылкой к ограниченному режиму
- [x] 5.4 Дополнить раздел «Границы» запретом обходных путей
- [x] 5.5 Проверить, что `SKILL.md` остаётся обозримым, а подробности ушли в reference

## 6. Установщик и документация

- [x] 6.1 Добавить в `install.sh` проверку версии интерпретатора с выводом найденной и
      требуемой
- [x] 6.2 Описать в `README.md` требования к окружению и поведение при их невыполнении
- [x] 6.3 Отметить в `README.md`, что установщик не обязателен и скилл работает при
      копировании директории

## 7. Проверка

- [x] 7.1 Прогнать разбор на тестовом логе в полном режиме — убедиться, что предпроверка
      добавляет один шаг и ничего не ломает
- [x] 7.2 Прогнать разбор с недоступным `python3` — убедиться, что скилл объявляет режим,
      не пытается обойти ограничение и держит вывод на уровне гипотезы
- [x] 7.3 Прогнать разбор без `git` — убедиться, что ограничивается только контур кода
- [x] 7.4 Прогнать `openspec validate` по изменению
