## 1. Проверить установку на целевой платформе

- [ ] 1.1 Проверить, загружает ли GigaCode проектные скиллы из `.gigacode/skills/` без
      дополнительного подтверждения доверия к директории; действующее требование
      «Установка под автономный профиль» фиксирует обратное поведение для клиентов
      семейства Qwen Code
      — **не выполнено:** ни `gigacode`, ни `qwen` в среде разработки нет, проверить
      нечем. Оговорка 1.2 добавлена как предосторожность, снимается первым прогоном
- [x] 1.2 Если подтверждение доверия требуется — добавить в README оговорку рядом с
      основным примером, а не менять основной путь

## 2. Рецепт установки по ссылке в README

- [x] 2.1 «Быстрый старт»: добавить рецепт с временным клонированием, определением
      директории скилла и копированием в `.gigacode/skills/`
- [x] 2.2 Оставить URL параметром команды: жёсткий URL внутреннего репозитория в README
      запрещён действующим требованием
- [x] 2.3 В команде поиска отсечь точечные директории — иначе кандидатами становятся
      двенадцать `SKILL.md` из `.qwen/skills/` и `.claude/skills/`
- [x] 2.4 Не ограничивать поиск по глубине: у распакованной выгрузки появляется лишний
      корневой каталог, и глубина директории скилла отличается от клона
- [x] 2.5 Сохранить в тексте команды пояснение, что копируется директория с `SKILL.md`,
      а не корень репозитория
- [x] 2.6 Сказать, что рецепт одинаков для публичного хостинга и внутреннего контура:
      меняется только подставляемая ссылка
- [x] 2.7 Добавить строку про случай, когда `git clone` не проходит (авторизация, сеть,
      сертификат): распаковать выгрузку во временную директорию, дальше шаги те же
- [x] 2.8 Сделать проектную установку первым примером и сказать, что база знаний копится
      по проекту; личную директорию оставить рядом как вариант
- [x] 2.9 Сохранить предупреждение про обновление поверх существующей установки и записи
      `kb/INC-*.md`

## 3. Реестр платформ

- [x] 3.1 `references/platforms.md`: отметить проектную директорию GigaCode как основной
      путь установки; роли и состав остальных платформ не трогать

## 4. Синхронизация

- [x] 4.1 После реализации синхронизировать дельту `skill-packaging` в
      `openspec/specs/skill-packaging/spec.md`
- [x] 4.2 Если меняются команды установки в каноне проекта, обновить `AI_CONTEXT.md` в том
      же изменении

## 5. Проверка

- [x] 5.1 Выполнить рецепт из README на этом репозитории и убедиться, что установлен
      `incident-detective`, а не одна из директорий оснастки
- [x] 5.2 `npx @fission-ai/openspec@latest validate --strict`
- [x] 5.3 `python3 tools/check_docs.py`
- [x] 5.4 `python3 tests/run.py`
