## 1. Подготовка

- [x] 1.1 Проверить, что работаем поверх архивного результата
      `improve-presentation-flow` (архивировано 29.07.2026): состав слайдов итоговый,
      подгонка под 1024×768 на месте
- [ ] 1.2 Зафиксировать точку отката: чистое дерево, снимок текущего вида — печать в
      PDF до правок, чтобы сравнить раздатку после — точка отката зафиксирована (worktree
      от чистого дерева на `cef29ce`), но физический PDF-снимок «до» не снят; сравнение
      в 6.3 сделано текстово (блок `facts`, разметка), не попиксельно

## 2. Палитра

- [x] 2.1 Заменить значения в `:root` на экранную палитру СберТеха: `--bg #01101B`,
      `--card`/`--code-bg #091D33`, `--line #32384A`, `--fg #FFFFFF`, `--muted #878C9D`,
      `--accent #56D7FB`; добавить `--fg-secondary #DCEFFD`, `--accent-2 #B51122` и
      `--bg-art-opacity`
- [x] 2.2 Удалить ветку `@media (prefers-color-scheme: dark)` целиком
- [x] 2.3 В `@media print` переопределить те же переменные на печатные:
      `--bg #FFFFFF`, `--fg #111111`, `--card`/`--code-bg #F2F2F2`, `--line #999999`,
      `--muted #444444`, `--accent #002C6F`, `--accent-2 #6E0711`. Отдельных печатных
      правил под селекторы не добавлять — дополнительно переопределён `--fg-secondary`
      на `#111111`, иначе метки `.tag` печатались бы светлым по белому
- [x] 2.4 Акцент применять скупо, как на сайте: `--accent` — заголовки `h2` и ключевые
      числа; метки `.tag` — `--fg-secondary` в рамке `--line`, без заливки акцентом.
      `--accent-2` содержания не набирает вовсе: контраст `#B51122` на `#01101B`
      около 2.5:1
- [x] 2.5 Ветка `prefers-color-scheme` удалена (2.2) — экран всегда тёмный независимо
      от системной темы, проверять на этот счёт больше нечего

## 3. Фоновый слой

- [x] 3.1 Добавить правила `.slide-bg` (absolute, inset 0, z-index 0, pointer-events
      none, `opacity: var(--bg-art-opacity)`), поднять содержимое слайда
      `position: relative; z-index: 1`, скрыть `.slide-bg` в `@media print` — плюс
      явные `width/height: 100%`, без них `<svg>` как replaced-элемент не растягивался
      по `inset:0` и держал дефолтные 300×150
- [x] 3.2 Завести общий контейнер `<svg>` с `<symbol>` в конце `<body>`, скрытый из
      потока и от скринридера. Атрибут `xmlns` не ставить нигде: `EXTERNAL_HOST_RE`
      в `tools/check_docs.py:60-65` ловит любое `http://…` и уронит проверку
- [x] 3.3 Подключение фона слайдом: `<svg class="slide-bg" aria-hidden="true"><use
      href="#bg-…"></svg>` первым ребёнком секции

## 4. Мотив «схема-водяной знак»

- [x] 4.1 `#bg-chain` — лента шагов, слайд «Как проходит разбор»
- [x] 4.2 `#bg-contours` — три контура, сходящиеся в узел сигнатуры, слайд «Три контура
      и что их связывает»
- [x] 4.3 `#bg-confidence` — шкала 0—1 с засечками на 0.40 и 0.70, слайд «Почему выводу
      можно верить». Засечки без подписей: числа остаются в таблице слайда — исходный
      viewBox 800×120 при `slice`-масштабировании под слайд почти целиком уезжал за
      кадр (масштаб по высоте зашкаливал), приведён к 800×600 как у остальных мотивов
- [x] 4.4 Схемы рисуются обводками, без крупных заливок; ни текста, ни чисел внутри

## 5. Мотив «строки лога»

- [x] 5.1 `#bg-loglines` — строки, скопированные дословно из
      `tests/fixtures/logs/payment.log`, с одной тлеющей `ERROR`-строкой
      (`ConnectionTimeoutError`) цветом `--accent-2`. Этой строке задать непрозрачность
      выше остальных: тёмно-красный по тёмно-синему на общем `--bg-art-opacity`
      пропадает — глазами подтверждено при `--bg-art-opacity: 0.05`
- [x] 5.2 Подключить на титуле, «Примере 1» и «Примере 2»; остальные слайды оставить
      без фона
- [x] 5.3 Убедиться, что фоновый текст не читается как содержание: кегль и контраст
      таковы, что он воспринимается фактурой — исходные `--bg-art-opacity: 0.1` /
      построчная `opacity: 0.5` читались как содержание, снижены до `0.05` / `0.35`

## 6. Приёмка

- [x] 6.1 1024×768 (проектор): ни один слайд не прокручивается ни по вертикали, ни по
      горизонтали. Это отдельная проверка, а не следствие «на моём мониторе всё влезло»
      — проверено программно (`scrollHeight`/`scrollWidth` == `client*` на всех 11)
- [x] 6.2 1280×800 и низкое окно (высота ≤ 700): те же проверки — 1280×800 и 1280×650,
      программно, расхождений нет
- [x] 6.3 Печать в PDF: фон светлый, текст тёмный, фоновой графики нет, акцентные
      элементы читаемы, каждый слайд — отдельная страница. Сравнить с распечаткой из
      задачи 1.2 на предмет потерянного содержания — проверено эмуляцией `@media print`
      (Playwright `emulateMedia`), не реальным экспортом в PDF; попиксельного сравнения
      с «до» нет (см. 1.2)
- [ ] 6.4 Печать в чёрно-белом режиме: акцентные элементы отличимы от основного текста
      — не проверено; `--accent #002C6F` и `--fg #111111` близки по яркости, при
      истинном ч/б преобразовании контраст акцента под вопросом
- [x] 6.5 `python3 tools/check_docs.py` — ноль расхождений, в том числе ни одного
      «обращение к внешнему хосту»
- [x] 6.6 `python3 tests/run.py` — зелёный (модуля `pytest` в окружении нет, набор
      гоняется штатным раннером репозитория); при падении не ослаблять тест, а
      править презентацию
- [x] 6.7 Блок `facts` не изменился: числа не добавлены и не удалены (`git diff`
      по блоку) — diff пуст внутри блока
- [x] 6.8 Прогнать презентацию глазами целиком в тёмной теме: проверить, что фон нигде
      не спорит с текстом и не выглядит грязью на слайдах с блоками кода — «Пример 2»
      (плотные блоки кода) проверен отдельно, фон не мешает

## 7. Хвосты

- [ ] 7.1 Показать результат на экране и решить, остаётся ли акцент циановым или
      переводится на зелёный `#2AB196` из того же CSS — это правка блока переменных и
      повторный проход 6.1—6.4. Решение за докладчиком, не принято в одностороннем порядке
- [x] 7.2 Проверить блоки кода и таблицы на слабом проекторе: если `#091D33` сливается
      с `#01101B`, различимость держать рамкой `--line`, а не заливкой — по скриншотам
      `--card`/`--code-bg` отличимы от фона рамкой `--line`, заливкой не полагались
- [x] 7.3 Точечная правка текста слайдов вне заявленного Non-Goals «содержание не
      трогаем» — по запросу в ходе показа результата. Проверка на признаки ИИ-текста
      (skill `human-copy-check`) нашла: метку `<span class="tag">точка решения</span>`
      (переименована в «развилка» — самодельный термин без расшифровки); высокую
      плотность тире и конструкции «не X, а Y» по всему документу (правки точечные,
      не сплошной рерайт — два самых заметных места на слайдах «Что делает скилл» и
      «Пример: автономный разбор от алерта»). Блок `facts` и числа не затронуты;
      `check_docs.py` и `tests/run.py` зелёные после правки
