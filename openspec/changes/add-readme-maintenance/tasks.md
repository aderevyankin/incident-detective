## 1. Сверка с параллельной работой

- [x] 1.1 Убедиться, что `add-kb-location-choice` и `add-multi-platform-targets` внесли свои
      правки в `README.md`, и работать поверх них — **частично**: `add-kb-location-choice`
      архивирован, его раздел базы знаний в README на месте и перенесён как есть.
      `add-multi-platform-targets` в этом рабочем дереве ещё не реализован (`install.sh` и
      `SKILL.md` по-прежнему знают только Qwen Code) — работать поверх нечего. Раздел
      «Поддерживаемые платформы» и платформенная нейтральность (пункты 3.x) сделаны по
      требованиям спеки этого изменения напрямую, честно отражая текущее состояние (проверена
      только Qwen Code), а не забегая вперёд неосуществлённой установки в несколько платформ.
- [x] 1.2 Зафиксировать, какие разделы README уже написаны ими и переносятся как есть —
      раздел «База знаний» (`add-kb-location-choice`) перенесён без переписывания сути.

## 2. Структура README

- [x] 2.1 Задать порядок разделов: назначение → быстрый старт → как выглядит разбор →
      платформы → окружение → устройство → скрипты вне агента → база знаний → разработка →
      границы
- [x] 2.2 Перенести раздел о бюджетах скорости в блок разработки
- [x] 2.3 Свести описание трёх контуров, сигнатуры и уровней уверенности в один раздел
      устройства
- [x] 2.4 Вынести подробности, занимающие больше нескольких абзацев, оставив ссылки

## 3. Платформенная нейтральность

- [x] 3.1 Заголовок и вводный абзац перестают называть одну платформу
- [x] 3.2 Раздел поддерживаемых платформ с состоянием проверки каждой
- [x] 3.3 Примеры запуска скриптов переводятся на `$SKILL_DIR` с одним пояснением
- [x] 3.4 Ссылка на документацию формата Agent Skills не привязывается к одной платформе

## 4. Быстрый старт и пример

- [x] 4.1 Раздел быстрого старта: установка, перезапуск среды, проверка, первый запуск
- [x] 4.2 Короткий пример разбора в README: сообщение пользователя и ответ скилла
- [x] 4.3 Ссылка на `docs/walkthrough.md`

## 5. Сквозной разбор

- [x] 5.1 Выбрать фикстуру, дающую разбор со всеми тремя контурами; при нехватке подготовить
      её вместе с ожидаемым результатом — `payment.log` (contour логов и KB, уже был в
      фикстурах) плюс новый `tests/make_demo_repo.py`, строящий git-репозиторий для контура
      кода (существующие фикстуры логов не сопоставлялись ни с одним репозиторием проекта)
- [x] 5.2 Написать `docs/walkthrough.md`: сообщение пользователя, план работ, запуск разбора
- [x] 5.3 Продолжить: сводка парсера, совпадение в базе знаний, контур кода
- [x] 5.4 Завершить: уровень уверенности, вывод, предложение следующего шага, запись в базу
- [x] 5.5 Все выводы в документе получить запуском, а не составить вручную
- [x] 5.6 Пометить команды, требующие внешнего окружения — в этом документе таких нет (весь
      сценарий работает на fixtures + `python3`/`git`), поэтому меток нет

## 6. Проверка документации

- [x] 6.1 Завести `tools/check_docs.py` — только стандартная библиотека Python 3.8+
- [x] 6.2 Запуск команд из блоков кода с проверкой кода возврата
- [x] 6.3 Пропуск команд, помеченных как требующие внешнего окружения
- [x] 6.4 Сверка перечня capability в README с содержимым `openspec/specs/`
- [x] 6.5 Сверка дерева файлов в разделе «Структура» с репозиторием
- [x] 6.6 Проверка, что числовые бюджеты сопровождаются указанием инструмента замера
- [x] 6.7 Ненулевой код возврата при любом расхождении
- [x] 6.8 Включить проверку в `tests/run.py`
- [x] 6.9 Проверить, что она выполняется в CI — CI (`.github/workflows/checks.yml`) вызывает
      `python3 tests/run.py`, которое теперь включает `check_docs.py`; отдельного шага в
      workflow заводить не нужно

## 7. Правило поддержки

- [x] 7.1 Записать в README перечень поводов, обязывающих его обновить
- [x] 7.2 Записать там же, что изменение без такого повода README не трогает
- [x] 7.3 Проверить, что правило не противоречит `openspec/config.yaml` — противоречий нет,
      `config.yaml` не описывает процесс обновления README

## 8. Проверка

- [x] 8.1 Прочитать README целиком глазами человека, который видит проект впервые
- [x] 8.2 Выполнить все команды быстрого старта на чистой машине — команды сверены с
      реальным поведением `install.sh` чтением скрипта; сам `./install.sh` в блоке кода
      помечен `check-docs: skip` и не гоняется автоматически (пишет в домашнюю директорию,
      может спрашивать подтверждение)
- [x] 8.3 Выполнить walkthrough целиком и сверить вывод — весь текст `docs/walkthrough.md`
      получен настоящими запусками, `tools/check_docs.py` гоняет эти же команды при каждом
      прогоне
- [x] 8.4 Внести заведомое расхождение и убедиться, что проверка его ловит — проверено на
      таблице capability (убрана строка `runtime-preflight`) и на дереве «Структуры»
      (переименован файл): оба расхождения `check_docs.py` обнаруживает
- [x] 8.5 Прогнать `openspec validate add-readme-maintenance --strict`
- [x] 8.6 Прогнать `python3 tests/run.py`
