# knowledge-base

## Purpose

Локальная база знаний по инцидентам: хранение разобранных случаев в markdown, поиск по
симптомам и по сигнатурам из логов, пополнение по итогам разбора. Замыкает цикл — второй
такой же инцидент разбирается не с нуля.

## Requirements

### Requirement: Хранение записей в markdown

База знаний SHALL храниться как markdown-файлы, по файлу на инцидент. Файлы SHALL быть
источником правды, а `index.json` — производным артефактом, пересобираемым из них.

#### Scenario: Расположение базы

- **WHEN** путь не задан явно
- **THEN** используется директория `kb/` внутри скилла
- **AND** путь переопределяется переменной окружения `INCIDENT_KB_DIR` или флагом `--kb`

#### Scenario: Индекс пересобирается

- **WHEN** записи правились вручную и запускается `kb_index.py`
- **THEN** `index.json` перестраивается из markdown-файлов
- **AND** содержит перечень записей, а также сводки по тегам, стендам и сервисам

#### Scenario: Индекс потерян

- **WHEN** `index.json` удалён
- **THEN** поиск по базе продолжает работать
- **AND** индекс восстанавливается пересборкой

### Requirement: Структура записи инцидента

Запись SHALL состоять из YAML-фронтматтера и разделов «Симптомы», «Диагностика»,
«Причина», «Решение», «Проверка», «Заметки».

#### Scenario: Обязательные поля фронтматтера

- **WHEN** запись создана
- **THEN** она содержит `id` вида `INC-YYYY-MM-NNN`, `title`, `date`, `stands` и `tags`

#### Scenario: Необязательные поля

- **WHEN** данные известны
- **THEN** запись дополняется полями `services`, `severity`, `status`, `signatures`,
  `files`, `commits`, `related`

#### Scenario: Идентификатор уникален

- **WHEN** создаётся новая запись
- **THEN** номер выбирается следующим свободным в пределах текущего месяца

#### Scenario: Разбор фронтматтера без внешних библиотек

- **WHEN** запись читается скриптом
- **THEN** фронтматтер разбирается встроенным парсером
- **AND** поддерживаются как инлайновые списки `[a, b]`, так и блочные через `- `

### Requirement: Поиск по свободному тексту

База знаний SHALL поддерживать поиск по симптому, сформулированному словами пользователя,
с учётом словоформ русского и английского языков.

#### Scenario: Совпадение по словоформе

- **WHEN** пользователь ищет «таймауты», а в записи указано «таймаут»
- **THEN** запись находится

#### Scenario: Взвешивание полей

- **WHEN** совпадение найдено в разных частях записи
- **THEN** заголовок и теги дают больший вклад в релевантность, чем текст разделов
- **AND** результаты сортируются по убыванию релевантности

#### Scenario: Фильтры

- **WHEN** заданы `--stand`, `--service` или `--tag`
- **THEN** записи без совпадения по этим полям исключаются из выдачи

#### Scenario: Совпадений нет

- **WHEN** ни одна запись не набрала релевантности
- **THEN** сообщается, что инцидент выглядит новым
- **AND** предлагается записать его после разбора

### Requirement: Поиск по сигнатурам из логов

База знаний SHALL искать по сигнатурам, извлечённым парсером логов, и SHALL оценивать
такое совпадение выше текстового.

#### Scenario: Поиск по результату разбора

- **WHEN** запускается `kb_search.py --from-parsed <файл>`
- **THEN** сигнатуры и классы исключений берутся из JSON-вывода парсера
- **AND** совпадение сигнатуры даёт существенно больший вклад в релевантность, чем
  совпадение отдельных слов

#### Scenario: Частичное совпадение сигнатуры

- **WHEN** сигнатуры не совпадают дословно, но пересекаются по составу
- **THEN** вычисляется степень схожести
- **AND** в выдаче указывается процент совпадения и сама сигнатура

#### Scenario: Совпадение не выдаётся за доказательство

- **WHEN** найдена запись по сигнатуре
- **THEN** в выдаче присутствует предупреждение, что совпадение не доказывает ту же
  причину и нужно сверить стенд, сервис и условия

### Requirement: Пополнение базы по итогам разбора

Скилл SHALL предлагать записать разбор и SHALL дополнять существующую запись вместо
создания дубля, если инцидент уже известен.

#### Scenario: Согласие пользователя

- **WHEN** разбор завершён и пользователь сам не просил записать
- **THEN** скилл спрашивает, записывать ли инцидент

#### Scenario: Сигнатуры сохраняются автоматически

- **WHEN** запись создаётся с `--from-parsed`
- **THEN** сигнатуры из разбора попадают в поле `signatures`
- **AND** по ним запись найдётся при следующем таком же инциденте

#### Scenario: Точка входа в код сохраняется

- **WHEN** причина локализована в коде
- **THEN** запись дополняется полями `files` и `commits`
- **AND** эти данные показываются в результатах поиска

#### Scenario: Повтор известного инцидента

- **WHEN** запускается `kb_add.py --update <id>` с новым стендом
- **THEN** стенд добавляется в список без дублирования
- **AND** в разделе «Заметки» фиксируется факт повтора с датой

#### Scenario: Дополнение текстовых разделов

- **WHEN** существующая запись дополняется новым текстом раздела
- **THEN** прежний текст сохраняется
- **AND** новый добавляется с пометкой даты

#### Scenario: Предпросмотр без записи

- **WHEN** задан флаг `--dry-run`
- **THEN** результат выводится на экран
- **AND** файлы не изменяются

### Requirement: Защита от утечки секретов

Запись в базу знаний SHALL вычищать очевидные секреты из сохраняемого текста. Скилл SHALL
записывать шаблоны и сигнатуры, а не сырые дампы логов.

#### Scenario: Секрет в тексте разбора

- **WHEN** сохраняемый текст содержит фрагмент вида `token=abc123secret` или `password=...`
- **THEN** значение заменяется на `<redacted>`

#### Scenario: Сырые дампы не сохраняются

- **WHEN** формируется запись
- **THEN** в неё попадают шаблоны сообщений и сигнатуры
- **AND** полные выгрузки логов в базу не переносятся

### Requirement: Порядок обращения к базе знаний

Скилл SHALL обращаться к базе знаний дважды: до получения логов — по словам пользователя,
и после разбора — по сигнатурам.

#### Scenario: Ранний поиск до логов

- **WHEN** карточка инцидента собрана
- **THEN** выполняется поиск по симптому до обращения к источнику логов
- **AND** при точном совпадении разбор может завершиться раньше

#### Scenario: Точный поиск после разбора

- **WHEN** логи разобраны и получены сигнатуры
- **THEN** выполняется повторный поиск по сигнатурам
