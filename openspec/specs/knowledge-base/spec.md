# knowledge-base

## Purpose

Локальная база знаний по инцидентам: хранение разобранных случаев в markdown, поиск по
симптомам и по сигнатурам из логов, пополнение по итогам разбора. Замыкает цикл — второй
такой же инцидент разбирается не с нуля.
## Requirements
### Requirement: Хранение записей в markdown

База знаний SHALL храниться как markdown-файлы, по файлу на запись. База SHALL хранить
записи двух видов — разбор инцидента и карта источника — и SHALL их различать: вид записи
задаётся явным полем, а поиск и выдача не смешивают их между собой. Файлы SHALL быть
источником правды, а `index.json` — производным артефактом, пересобираемым из них.
Расположение базы SHALL определяться выбором пользователя, а директория внутри скилла
SHALL оставаться значением по умолчанию до того, как выбор сделан.

#### Scenario: Расположение базы

- **WHEN** путь не задан явно и в корне текущего репозитория базы нет
- **THEN** используется директория `kb/` внутри скилла
- **AND** путь переопределяется переменной окружения `INCIDENT_KB_DIR` или флагом `--kb`

#### Scenario: База выбрана в проекте

- **WHEN** путь не задан явно, но в корне текущего репозитория база уже существует
- **THEN** используется она

#### Scenario: Расположение не выбрано

- **WHEN** путь не задан, базы в корне репозитория нет и выбор пользователем ещё не сделан
- **THEN** чтение базы работает с директорией внутри скилла
- **AND** признак «расположение не выбрано» доступен скиллу, чтобы задать вопрос при записи

#### Scenario: Вид записи различается

- **WHEN** индекс пересобирается по базе, где лежат и разборы инцидентов, и карта
  источников
- **THEN** каждая запись получает в индексе свой вид
- **AND** поиск по инцидентам не возвращает записи карты, а запрос карты не возвращает
  разборы

#### Scenario: Запись без явного вида

- **WHEN** в базе оказалась запись, у которой вид не указан
- **THEN** она считается разбором инцидента
- **AND** база, собранная предыдущими версиями скилла, остаётся читаемой

#### Scenario: Индекс пересобирается

- **WHEN** записи правились вручную и запускается `kb_index.py`
- **THEN** `index.json` перестраивается из markdown-файлов
- **AND** содержит перечень записей, а также сводки по тегам, стендам и сервисам

#### Scenario: Индекс потерян

- **WHEN** `index.json` удалён
- **THEN** поиск по базе продолжает работать
- **AND** индекс восстанавливается пересборкой

### Requirement: Структура записи инцидента

Запись SHALL состоять из YAML-фронтматтера и разделов «Симптомы», «Диагностика»,
«Причина», «Решение», «Проверка», «Заметки». Запись SHALL хранить исход разбора отдельно от
состояния инцидента.

#### Scenario: Обязательные поля фронтматтера

- **WHEN** запись создана
- **THEN** она содержит `id` вида `INC-YYYY-MM-NNN`, `title`, `date`, `stands` и `tags`

#### Scenario: Необязательные поля

- **WHEN** данные известны
- **THEN** запись дополняется полями `services`, `severity`, `status`, `signatures`,
  `files`, `commits`, `related`

#### Scenario: Исход разбора

- **WHEN** известно, подтвердилась ли названная причина на практике
- **THEN** запись содержит исход в одном из трёх значений: подтверждена, опровергнута,
  не проверена
- **AND** исход хранится отдельно от `status` и не выводится из него

#### Scenario: Исход не указан

- **WHEN** запись не содержит поля исхода
- **THEN** она читается без ошибки
- **AND** исход трактуется как «не проверена»

#### Scenario: Переиспользование записи

- **WHEN** запись пригодилась при разборе повтора инцидента
- **THEN** она содержит счётчик переиспользования и дату последнего переиспользования

#### Scenario: Идентификатор уникален

- **WHEN** создаётся новая запись
- **THEN** номер выбирается следующим свободным в пределах текущего месяца

#### Scenario: Разбор фронтматтера без внешних библиотек

- **WHEN** запись читается скриптом
- **THEN** фронтматтер разбирается встроенным парсером
- **AND** поддерживаются как инлайновые списки `[a, b]`, так и блочные через `- `

### Requirement: Поиск по свободному тексту

База знаний SHALL поддерживать поиск по симптому, сформулированному словами пользователя,
с учётом словоформ русского и английского языков.

#### Scenario: Совпадение по словоформе

- **WHEN** пользователь ищет «таймауты», а в записи указано «таймаут»
- **THEN** запись находится

#### Scenario: Взвешивание полей

- **WHEN** совпадение найдено в разных частях записи
- **THEN** заголовок и теги дают больший вклад в релевантность, чем текст разделов
- **AND** результаты сортируются по убыванию релевантности

#### Scenario: Фильтры

- **WHEN** заданы `--stand`, `--service` или `--tag`
- **THEN** записи без совпадения по этим полям исключаются из выдачи

#### Scenario: Совпадений нет

- **WHEN** ни одна запись не набрала релевантности
- **THEN** сообщается, что инцидент выглядит новым
- **AND** предлагается записать его после разбора

### Requirement: Поиск по сигнатурам из логов

База знаний SHALL искать по сигнатурам, извлечённым парсером логов, и SHALL оценивать
такое совпадение выше текстового.

#### Scenario: Поиск по результату разбора

- **WHEN** запускается `kb_search.py --from-parsed <файл>`
- **THEN** сигнатуры и классы исключений берутся из JSON-вывода парсера
- **AND** совпадение сигнатуры даёт существенно больший вклад в релевантность, чем
  совпадение отдельных слов

#### Scenario: Частичное совпадение сигнатуры

- **WHEN** сигнатуры не совпадают дословно, но пересекаются по составу
- **THEN** вычисляется степень схожести
- **AND** в выдаче указывается процент совпадения и сама сигнатура

#### Scenario: Совпадение не выдаётся за доказательство

- **WHEN** найдена запись по сигнатуре
- **THEN** в выдаче присутствует предупреждение, что совпадение не доказывает ту же
  причину и нужно сверить стенд, сервис и условия

### Requirement: Пополнение базы по итогам разбора

Скилл SHALL предлагать записать разбор и SHALL дополнять существующую запись вместо
создания дубля, если инцидент уже известен.

#### Scenario: Согласие пользователя

- **WHEN** разбор завершён и пользователь сам не просил записать
- **THEN** скилл спрашивает, записывать ли инцидент

#### Scenario: Сигнатуры сохраняются автоматически

- **WHEN** запись создаётся с `--from-parsed`
- **THEN** сигнатуры из разбора попадают в поле `signatures`
- **AND** по ним запись найдётся при следующем таком же инциденте

#### Scenario: Точка входа в код сохраняется

- **WHEN** причина локализована в коде
- **THEN** запись дополняется полями `files` и `commits`
- **AND** эти данные показываются в результатах поиска

#### Scenario: Повтор известного инцидента

- **WHEN** запускается `kb_add.py --update <id>` с новым стендом
- **THEN** стенд добавляется в список без дублирования
- **AND** в разделе «Заметки» фиксируется факт повтора с датой

#### Scenario: Дополнение текстовых разделов

- **WHEN** существующая запись дополняется новым текстом раздела
- **THEN** прежний текст сохраняется
- **AND** новый добавляется с пометкой даты

#### Scenario: Предпросмотр без записи

- **WHEN** задан флаг `--dry-run`
- **THEN** результат выводится на экран
- **AND** файлы не изменяются

### Requirement: Защита от утечки секретов

Запись в базу знаний SHALL вычищать секреты и персональные данные из сохраняемого текста.
Скилл SHALL записывать шаблоны и сигнатуры, а не сырые дампы логов. Очистка SHALL выполняться
общей функцией, используемой всеми путями записи, и SHALL NOT дублироваться отдельными
реализациями.

#### Scenario: Секрет в тексте разбора

- **WHEN** сохраняемый текст содержит фрагмент вида `token=abc123secret` или `password=...`
- **THEN** значение заменяется на `<redacted>`

#### Scenario: Секрет после именованного префикса из двух слов

- **WHEN** сохраняемый текст содержит `Authorization: Bearer <токен>` или иную форму,
  где между именем поля и значением стоит служебное слово
- **THEN** маскируется сам токен, а не служебное слово

#### Scenario: Очистка охватывает все поля записи

- **WHEN** секрет или персональные данные попали в заголовок, сигнатуру или значения,
  извлечённые из разбора логов
- **THEN** они маскируются так же, как в текстах секций

#### Scenario: Персональные данные в тексте разбора

- **WHEN** сохраняемый текст содержит адрес электронной почты, номер телефона, номер платёжной
  карты, номер счёта или идентификатор физического лица
- **THEN** значение заменяется меткой, называющей вид данных
- **AND** само значение в записи не сохраняется

#### Scenario: Маскирование сохраняет читаемость

- **WHEN** значение замаскировано
- **THEN** на его месте остаётся метка вида данных, а не пустое место
- **AND** для номера платёжной карты сохраняются последние четыре цифры

#### Scenario: Одинаковые значения маскируются одинаково

- **WHEN** одно и то же значение встречается в записи несколько раз
- **THEN** во всех местах подставляется одна и та же метка
- **AND** сопоставление метки с исходным значением никуда не сохраняется

#### Scenario: Номер карты проверяется контрольной суммой

- **WHEN** в тексте встречается последовательность цифр, похожая на номер карты
- **THEN** она маскируется как карта только при прохождении проверки контрольной суммой
- **AND** идентификатор, не прошедший проверку, остаётся нетронутым

#### Scenario: Сырые дампы не сохраняются

- **WHEN** формируется запись
- **THEN** в неё попадают шаблоны сообщений и сигнатуры
- **AND** полные выгрузки логов в базу не переносятся
- **AND** наличие очистки не считается основанием сохранять логи целиком

### Requirement: Порядок обращения к базе знаний

Скилл SHALL обращаться к базе знаний дважды: до получения логов — по словам пользователя,
и после разбора — по сигнатурам.

#### Scenario: Ранний поиск до логов

- **WHEN** карточка инцидента собрана
- **THEN** выполняется поиск по симптому до обращения к источнику логов
- **AND** при точном совпадении разбор может завершиться раньше

#### Scenario: Точный поиск после разбора

- **WHEN** логи разобраны и получены сигнатуры
- **THEN** выполняется повторный поиск по сигнатурам

### Requirement: Поиск опирается на индекс

Поиск по базе знаний SHALL использовать `index.json` и SHALL читать markdown-файлы записей
только тогда, когда индекса недостаточно — при его отсутствии, устарелости или при выводе
подробностей найденной записи. Сплошное чтение всех файлов базы на каждый запрос
запрещено: база растёт, а поиск вызывается на каждом разборе и должен укладываться в свой
бюджет времени.

#### Scenario: Обычный поиск

- **WHEN** выполняется поиск по тексту или по сигнатурам, а `index.json` актуален
- **THEN** релевантность считается по данным индекса
- **AND** markdown-файлы читаются только для записей, попавших в выдачу

#### Scenario: Индекс отсутствует или устарел

- **WHEN** `index.json` не найден или не соответствует набору файлов базы
- **THEN** поиск выполняется чтением markdown-файлов
- **AND** пользователю сообщается, что индекс стоит пересобрать

#### Scenario: Источник правды не меняется

- **WHEN** данные индекса расходятся с содержимым markdown-файла
- **THEN** правильным считается markdown-файл
- **AND** индекс остаётся производным артефактом

### Requirement: Выбор расположения базы знаний при первой записи

Скилл SHALL спрашивать пользователя, где держать базу знаний, когда расположение не задано
явно и выполняется первая запись. Вопрос SHALL задаваться инструментом `ask_user_question`
выбором из готовых вариантов и SHALL NOT требовать ручного ввода пути в вариантах, которые
скилл может составить сам.

#### Scenario: Вопрос при первой записи

- **WHEN** ни `--kb`, ни `INCIDENT_KB_DIR` не заданы и скилл собирается записать в базу
- **THEN** он спрашивает, где хранить базу, до создания записи
- **AND** вопрос задаётся вариантами через `ask_user_question`

#### Scenario: Готовые варианты

- **WHEN** задаётся вопрос о расположении
- **THEN** предлагается вариант «в корне проекта» с уже подставленным путём
- **AND** предлагается вариант «в директории скилла» — текущее поведение по умолчанию
- **AND** свой путь остаётся доступен свободным ответом

#### Scenario: Вопрос не задаётся в начале разбора

- **WHEN** разбор только начался и выполняется поиск по базе знаний
- **THEN** вопрос о расположении не задаётся
- **AND** поиск идёт по той базе, которая доступна сейчас

#### Scenario: Расположение задано явно

- **WHEN** задан `--kb` или `INCIDENT_KB_DIR`
- **THEN** вопрос не задаётся ни при поиске, ни при записи
- **AND** используется заданный путь

#### Scenario: Выбор для этого проекта уже сделан

- **WHEN** расположение не задано явно, но в корне текущего репозитория уже существует база
  знаний
- **THEN** вопрос не задаётся
- **AND** используется база этого репозитория

#### Scenario: В базе внутри скилла уже есть записи

- **WHEN** расположение не задано, но база внутри скилла уже содержит записи
- **THEN** вопрос не задаётся
- **AND** запись идёт туда же, где лежат существующие

#### Scenario: Пользователь отказался выбирать

- **WHEN** пользователь не выбрал расположение
- **THEN** запись идёт в базу внутри скилла
- **AND** в этом разборе вопрос повторно не задаётся

### Requirement: Запоминание выбранного расположения

Скилл SHALL сообщать пользователю, как закрепить выбор через `INCIDENT_KB_DIR`, и
SHALL NOT заводить собственный файл состояния для хранения пути.

#### Scenario: Готовая строка для профиля

- **WHEN** расположение выбрано
- **THEN** скилл показывает готовую строку `export INCIDENT_KB_DIR=<путь>`
- **AND** поясняет, что без неё вопрос возникнет в следующем разборе

#### Scenario: Своего файла состояния нет

- **WHEN** расположение выбрано
- **THEN** скилл не создаёт файлов с настройками ни в проекте, ни в своей директории

#### Scenario: Для базы в проекте строка не обязательна

- **WHEN** выбрана база в корне репозитория
- **THEN** скилл поясняет, что в этом проекте выбор будет прочитан по наличию директории и
  без строки `export`
- **AND** строка показывается как способ работать с этой базой из других директорий

### Requirement: Порядок разрешения пути к базе знаний

Путь к базе знаний SHALL разрешаться в порядке: флаг `--kb`, переменная `INCIDENT_KB_DIR`,
база в корне текущего репозитория при её существовании, база внутри скилла. Существующая
база в корне репозитория SHALL считаться ранее сделанным выбором и SHALL NOT создаваться в
ходе разрешения пути.

#### Scenario: Флаг сильнее всего

- **WHEN** задан `--kb`
- **THEN** используется указанный путь независимо от переменной окружения и от наличия базы
  в репозитории

#### Scenario: Переменная сильнее базы в проекте

- **WHEN** задана `INCIDENT_KB_DIR` и в корне текущего репозитория существует база
- **THEN** используется путь из переменной
- **AND** это остаётся способом намеренно работать с одной базой из разных репозиториев

#### Scenario: База проекта сильнее умолчания

- **WHEN** ни флаг, ни переменная не заданы и в корне текущего репозитория существует база
- **THEN** используется база этого репозитория, а не директория внутри скилла

#### Scenario: Работа с несколькими проектами

- **WHEN** пользователь закрепил `INCIDENT_KB_DIR` для одного проекта и разбирает инцидент в
  другом репозитории, где своя база уже создана
- **THEN** запись идёт в базу того репозитория, в котором ведётся разбор

#### Scenario: Базы в репозитории нет

- **WHEN** ни флаг, ни переменная не заданы и в корне репозитория базы нет
- **THEN** расположение считается невыбранным
- **AND** директория в репозитории при этом не создаётся

#### Scenario: Разбор вне репозитория

- **WHEN** текущая директория не является git-репозиторием
- **THEN** шаг с базой в корне репозитория пропускается
- **AND** разрешение продолжается директорией внутри скилла

#### Scenario: Источник пути известен скиллу

- **WHEN** путь разрешён
- **THEN** скиллу доступно, каким из шагов он получен
- **AND** по этому признаку определяется, задавать ли вопрос о расположении

#### Scenario: Путь записи назван пользователю

- **WHEN** запись добавлена в базу знаний
- **THEN** в ответе назван путь, куда она попала
- **AND** запись в базу репозитория, найденную по третьему шагу, не остаётся незамеченной

### Requirement: Согласие на создание директории базы

Скилл SHALL создавать директорию базы знаний только после выбора пользователя и SHALL
предупреждать о попадании записей в git, когда выбран путь внутри репозитория.

#### Scenario: Директория не создаётся молча

- **WHEN** расположение не выбрано
- **THEN** скилл не создаёт директорию базы ни в проекте, ни в другом месте

#### Scenario: Предупреждение о git

- **WHEN** выбран путь внутри репозитория проекта
- **THEN** скилл одной строкой сообщает, что записи попадут в git и из истории не
  вычищаются
- **AND** продолжает работу, а не отговаривает от выбора

#### Scenario: Путь вне репозитория

- **WHEN** выбран путь вне проекта
- **THEN** предупреждение о git не выводится

### Requirement: Перечень немаскируемых данных

Очистка SHALL NOT затрагивать данные, необходимые для разбора и не являющиеся персональными.
Перечень таких данных SHALL быть зафиксирован явно.

#### Scenario: Диагностические идентификаторы сохраняются

- **WHEN** в тексте встречаются идентификатор запроса, trace id, номер заказа или транзакции
- **THEN** они сохраняются без изменений

#### Scenario: Техническое окружение сохраняется

- **WHEN** в тексте встречаются имена сервисов и хостов, версии, пути в коде, IP-адреса
- **THEN** они сохраняются без изменений

#### Scenario: Запись остаётся пригодной для поиска

- **WHEN** запись прошла очистку
- **THEN** поиск по сигнатурам находит её так же, как до очистки
- **AND** извлечённые сигнатуры не содержат замаскированных фрагментов

### Requirement: Уведомление о срабатывании очистки

Скилл SHALL сообщать о срабатывании очистки при записи. Сообщение SHALL называть виды и
количество замаскированного и SHALL NOT содержать исходных значений.

#### Scenario: Очистка сработала

- **WHEN** при записи что-то было замаскировано
- **THEN** пользователю сообщается, какие виды данных и сколько раз были замаскированы
- **AND** исходные значения в сообщении не приводятся

#### Scenario: Предпросмотр показывает очищенный текст

- **WHEN** запись формируется с `--dry-run`
- **THEN** показывается текст в том виде, в каком он будет сохранён — уже с маскированием
- **AND** приводится сводка срабатываний

#### Scenario: Лишнее маскирование заметно пользователю

- **WHEN** очистка замаскировала фрагмент, нужный для разбора
- **THEN** это видно из сводки срабатываний до сохранения
- **AND** пользователь может исправить текст и повторить запись

#### Scenario: Очистка не сработала

- **WHEN** маскировать было нечего
- **THEN** запись выполняется без дополнительных сообщений

### Requirement: Отличительные признаки записи

Запись MAY содержать отличительные признаки — проверяемые наблюдения, по которым видно, что
текущий случай не является описанным, несмотря на совпадение сигнатуры. Признаки SHALL
формулироваться как наблюдения в данных, а не как указания провести исследование.

#### Scenario: Признак сформулирован проверяемо

- **WHEN** в запись добавляется отличительный признак
- **THEN** он называет конкретное наблюдение, которое можно проверить сразу — строку в логе,
  значение поля, состояние системы
- **AND** не сводится к общему указанию сверить условия

#### Scenario: Признаки видны в выдаче поиска

- **WHEN** запись найдена по сигнатуре или по тексту
- **THEN** её отличительные признаки показываются рядом с совпадением
- **AND** не требуют открытия файла записи целиком

#### Scenario: Признаков нет

- **WHEN** отличительных признаков у записи нет
- **THEN** запись остаётся валидной
- **AND** выдача не содержит пустого раздела

#### Scenario: Признак проверяется до принятия версии записи

- **WHEN** разбор нашёл похожую запись и собирается опираться на её версию причины
- **THEN** отличительные признаки проверяются до этого
- **AND** совпадение признака означает, что случай другой, и это называется в ответе

### Requirement: Исход и возраст записи в выдаче поиска

Выдача поиска SHALL показывать исход каждой найденной записи. Опровергнутая запись SHALL
подаваться как опровергнутая и SHALL NOT занимать в выдаче то же положение, что подтверждённая.

#### Scenario: Исход показан

- **WHEN** запись попала в выдачу
- **THEN** её исход указан в результате

#### Scenario: Опровергнутая запись

- **WHEN** в выдачу попадает запись с исходом «опровергнута»
- **THEN** она помечается явно
- **AND** располагается ниже подтверждённых и непроверенных
- **AND** не удаляется из выдачи, поскольку сообщает о непроверившейся гипотезе

#### Scenario: Возраст записи

- **WHEN** запись попала в выдачу
- **THEN** указан её возраст
- **AND** указан счётчик переиспользования, если он ненулевой

#### Scenario: Код менялся после записи

- **WHEN** поиск выполняется с указанием репозитория и запись содержит поле `files`
- **THEN** сообщается, менялись ли эти файлы после даты записи
- **AND** это подаётся как сведение к проверке, а не как вывод об устаревании

#### Scenario: Переиспользование не влияет на релевантность

- **WHEN** вычисляется релевантность записи
- **THEN** счётчик переиспользования в неё не входит

### Requirement: Фиксация исхода и переиспользования

Скилл SHALL фиксировать исход разбора, когда он становится известен, и SHALL увеличивать
счётчик переиспользования при подтверждённом повторе инцидента. Исход SHALL NOT определяться
автоматически.

#### Scenario: Исход указывается при записи

- **WHEN** создаётся запись по итогам разбора
- **THEN** исход можно указать сразу
- **AND** по умолчанию он «не проверена», поскольку фикс ещё не подтверждён практикой

#### Scenario: Исход обновляется позже

- **WHEN** стало известно, подтвердилась причина или нет
- **THEN** исход существующей записи обновляется с фиксацией даты
- **AND** прежнее содержимое записи сохраняется

#### Scenario: Повтор инцидента

- **WHEN** запускается обновление записи по факту повтора
- **THEN** счётчик переиспользования увеличивается
- **AND** обновляется дата последнего переиспользования

#### Scenario: Уточнение исхода по поводу

- **WHEN** разбор опирался на найденную запись и её версия подтвердилась или не подтвердилась
- **THEN** скилл предлагает обновить исход этой записи
- **AND** не опрашивает пользователя об исходе прочих записей базы

### Requirement: Поиск сигнатуры на других стендах

Поиск SHALL поддерживать запрос «та же сигнатура на других стендах» — выдачу записей с
совпадающей сигнатурой, исключая указанный стенд. Выдача SHALL называть стенд каждой
найденной записи, чтобы регрессия, переехавшая между стендами, была видна сразу.

#### Scenario: Ошибка уже встречалась на другом стенде

- **WHEN** запрошена сигнатура с исключением текущего стенда
- **THEN** возвращаются записи с этой сигнатурой по остальным стендам
- **AND** у каждой названы стенд, дата и исход

#### Scenario: Совпадений на других стендах нет

- **WHEN** сигнатура встречалась только на исключённом стенде
- **THEN** выдача пуста
- **AND** это сообщается как «на других стендах не встречалась», а не как отсутствие
  записей вообще

#### Scenario: Стенд не указан

- **WHEN** запрос сделан без указания исключаемого стенда
- **THEN** поиск ведёт себя как обычный поиск по сигнатуре
- **AND** ошибкой это не считается

